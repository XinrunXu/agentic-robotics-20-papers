"""补充阅读各篇的精读内容，字段与 deep_readings.py 一致。

同样的口径：实验数字都是作者报告，教学推演与研究练习是讲义编写的内容。
"""
EXTRA_NOTES = {}

def add(key, **note):
    EXTRA_NOTES[key] = note


add('vla-touch', version='v2',
 takeaway='不重训基座，触觉分两路进入系统：语义化后喂给规划器，力向量喂给一个自训的动作精修器。',
 result='三项真机任务各 20 次：完整擦拭从 RDT 的 5/20 升到插值控制器的 12/20，完整削皮从 6/20 升到 10/20。',
 why='VLA 的反馈通道几乎只有视觉与本体，抓没抓稳、表面滑不滑、按下去有多硬这几件事看不出来，把触觉喂进基座又缺大规模多模态数据。这篇的做法是不用触觉数据动基座权重，改在它的上游与下游各接一条触觉通路，分别检验触觉对规划与对控制各有多少用。',
 scene='桌上放着一只带盖的杯子，看不出里面有没有水；旁边两块海绵外观相同，其中一块更粗糙；还有两只青芒，熟的那只更软。只按画面下指令的话，机器人只能瞎猜该把杯子放到哪、该拿哪块海绵。真到削皮那一步，刀刃贴着果皮滑过去也没有人发现。',
 recall_module='改的是基座 VLA 的上游与下游，两头各挂一条触觉通路：上游让规划器多读一句语言化的触觉描述，下游让一个外挂控制器把动作块按接触力改写。基座权重在触觉这件事上一点没动，动作块的生成方式与指令接口都保持原样；规划器是现成的通用模型，不为机器人微调；触觉语义模型同样拿来即用。它没有加执行后的成功判定，也没有改变触觉进入系统的时间尺度——语义那一路是秒级，控制那一路封顶 8 Hz。',
 recall_io='输入分两路，两路看到的东西并不相同。规划器读顶置相机的场景图、任务目标，加上一句文本化的触觉描述：粗糙度与硬度由 Octopi 读 6 帧 GelSight 序列给出，力则不经过 Octopi，由 OpenCV 的 marker 跟踪算法在 7×9 标记阵列上算出合力向量与模长，再附经验参考值写成文本。插值控制器读的是冻结 DinoV2 的视觉嵌入、本体状态与那个低维力向量，历史长度 2。输出也是两级：规划器每轮写一条原语动作，交给 RDT-1B 生成 64 步动作块；控制器把前 48 步按非重叠窗口逐段改写，以 8 Hz 交给笛卡尔 PD 控制器执行，削芒果换阻抗控制，终止条件是规划器判定目标达成。失败这条通路是残缺的：算法 1 写了执行失败就跳出执行循环去重新规划，但论文没有给出谁判定这次失败、依据什么证据，既没有成功检测器，也没有恢复策略；实际能兜住的只有步数超限，以及下一轮规划器读到新的触觉描述后自己改主意。',
 steps=[('两条触觉通路，基座权重一点不动', '触觉被拆到两层。高层由 GPT-4o 充当规划器，读场景图与一句语言化的触觉描述，按提示每轮只写一条原语动作加一项要取回的信息；低层由 RDT-1B 生成 64 步动作块，再由一个外挂控制器按接触力改写。基座自始至终不吃触觉输入，标题所说的不微调，指的是不用触觉数据微调它。'), ('语义触觉：现成的 Octopi 把 6 帧触觉转成一句话', '触觉语言模型直接用现成的 Octopi，它为 GelSight 预训练，读入 6 帧触觉序列后给出粗糙度与硬度。力不在它的能力范围内，作者另用 OpenCV 的 marker 跟踪算法处理 7×9 标记阵列，得到合力向量与模长，连同经验参考值一起写成文本交给 GPT-4o。'), ('低层触觉：作者自训的插值控制器', '插值控制器沿用 BRIDGeR 的随机插值框架，把 VLA 的动作分布当源分布、专家动作当目标分布，以 MSE 监督训练，因此这一块是作者自训的。它的输入是冻结 DinoV2 给出的视觉嵌入、本体状态与那个低维力向量，历史长度取 2，它看不到语言。'), ('滑窗精修、执行，再回到规划器', '64 步动作块取前 48 步做精修，按非重叠窗口顺序处理，每执行完一窗就从上一窗终点接着往下走；控制器最高 8 Hz，执行也是 8 Hz，走笛卡尔 PD 控制，削芒果改用阻抗控制。一条指令跑完或步数超限，就取一次触觉观测交给 Octopi，再让规划器写下一条指令，直到目标达成。'), ('训练成本落在哪几处', '数据靠手把手示教采集，10 Hz，三项合计约 53 分钟：杯子 40 加 60 条，擦拭 40 加 60 条，削皮 60 加 120 条。RDT 先在无关 Franka 数据上微调 10 万步对齐动作空间，约 80 小时，再按任务在无触觉数据上各微调 2 万步、约 16 小时。')],
 example='教学推演：回到上面的场景。第一步，GPT-4o 拿到顶置相机的画面和一句目标，按提示只写出一条原语动作——先轻轻夹一下杯子把它提起来，并注明需要取回的信息是重量。第二步，RDT-1B 把这条指令变成 64 步动作块，前 48 步交给插值控制器：marker 跟踪在 7×9 标记阵列上算出合力向量，控制器据此改写夹取轨迹，再按非重叠窗口以 8 Hz 一窗接一窗执行完。第三步，这段执行结束后系统取一次触觉观测，力的模长与方向以文本形式送回规划器，附录那段对话给出的参考是空杯约 0.55、满杯约 1.1，于是规划器判定杯里有水，下一条指令改成放到盘子上。第四步，换到擦拭与削皮就轮到 Octopi 出场：它读 6 帧 GelSight 序列，给出粗糙度与硬度的数值，规划器据此挑较光滑的那块海绵、较软的那只芒果。第五步，选定之后再回到低层，削皮全程靠触觉维持压力，论文的图 4 数到插值控制器削下 13 条、其中只有 3 条断，残差控制器 12 条里断了 5 条。要留住的是这套循环没有判断刚才那一步做没做成的环节：算法里写了执行失败就退出去重新规划，但由谁判、依据什么，论文没有给出；杯子提前脱手、海绵抓在边缘，都只能等下一轮触觉描述或画面碰巧反映出来。',
 code='planner = GPT-4o（现成，只提示）; vla = RDT-1B（已用无触觉数据微调）\nI = planner(goal, scene_image)          # 每轮只出一条原语动作\nwhile 任务未完成:\n    while 指令 I 未完成:\n        a[0:64] = vla(s_t, I)           # 动作块，只取前 48 步送精修\n        for w in 非重叠切窗(a[0:48]):\n            m_t = marker_tracking(gelsight)      # 7×9 阵列 → 合力 (X, Y, M)\n            a_hat = interpolant(s_t, w, m_t)     # 作者自训，源分布=VLA 动作\n            执行(a_hat)                 # 8 Hz 笛卡尔 PD；削皮改阻抗控制\n            s_t, m_t = 更新观测()\n        if 执行失败: break              # 谁判、依据什么，论文没有给出\n    L = Octopi(最近 6 帧触觉)           # 现成预训练，给粗糙度与硬度\n    I = planner(goal, scene_image, L, I)  # 语言化触觉回到规划器',
 setup='硬件是 Franka Emika Panda 配 Robotiq 2F-140 夹爪，一指装 GelSight Mini，顶置与腕部各一台 RealSense，推理跑在单张 RTX 4090 上。三项真机任务为杯子、擦拭、削芒果；规划与操作对比都是每个条件 20 次，随机只在物体摆位与目标位置。',
 headers=['评测项 · 表 1', 'RDT', '残差控制器', '插值控制器', '插值去掉触觉'],
 rows=[['杯子 · 放置', '7/20', '6/20', '10/20', '5/20'], ['擦拭 · 完整', '5/20', '6/20', '12/20', '7/20'], ['削皮 · 完整', '6/20', '7/20', '10/20', '5/20'], ['擦拭 · 抓取', '11/20', '15/20', '17/20', '15/20']],
 findings='分母都是 20 次，一格差一次就是 5 个百分点，绝对量级都不大。抓取那一行说明触觉几乎没帮上忙，15/20 与 17/20 的差距在噪声量级内，收益集中在放置、完整擦拭、完整削皮这些持续接触的阶段。摘要里 42% 到 140% 的提升是相对值，回到原始计数只是 7 次变 12 次。',
 ablation='表 2 把两条通路分别关掉：擦拭整任务从 12/20 降到 5/20 与 5/20，削皮从 7/20 降到 6/20 与 4/20。表 1 后两列拆控制器的输入，去掉触觉后杯子放置从 10/20 掉到 5/20，去掉视觉 7/20。附录 B 发现离散语言标签不如连续分类器输出，削芒果 60% 对 75%。',
 boundary='模型来源要分三层算。规划侧 GPT-4o 是现成的通用模型，只用提示、不做机器人微调；触觉语义侧 Octopi 是现成的预训练触觉语言模型，视觉嵌入用的冻结 DinoV2 也是现成的；插值控制器由作者自训，靠手把手示教的约 53 分钟数据，这是真正的前提成本。基座 RDT-1B 的情况要说清楚：所谓不微调只指不用触觉数据微调，实际上作者先在无关 Franka 数据上跑了 10 万步、约 80 小时对齐动作空间，又按任务各跑 2 万步、约 16 小时，所以这套系统不是拿来即用。三项任务各配一个插值控制器与一份示教数据，论文自己也写明只检验了物体摆位与目标位置上的泛化。作者另列两条硬约束：夹爪设置与 Octopi 预训练数据不一致，硬度判断因此会偏；控制器只跑到 8 Hz，25 Hz 以上的高频触觉没被用上。',
 transfer='与 Harness VLA 对读：两篇都不改策略权重，那边换的是调用时机与起步状态，这边换的是证据来源；缺的仍是 PhyAgentOS 的独立验收。',
 exercise='在一条抓取流水线上把触觉分数分别只接进规划、只接进控制、两处都接，记录每一阶段各自的失败计数。',
 answer='要看的不是总成功率，而是失败有没有换位置：规划侧触觉应减少选错对象，控制侧应减少滑脱与提前松手，混在一个数字里就分不开。',
 sources=[('方法总览：§3', 'S3'), ('触觉辅助规划：§3.1', 'S3.SS1'), ('操作结果：表 1', 'S4.T1'), ('局限：§6', 'S6')])

add('touchguide', version='v6',
 takeaway='策略权重不动，只在去噪或流匹配的最后几步，用触觉可行性分数的梯度把动作推向符合接触物理的一侧。',
 result='五项真机任务各 20 次：π₀.₅ 平均 35.9%，触觉引导后 58.0%；Diffusion Policy 是 16.3% 到 36.2%。',
 why='视触融合过去多在特征层拼接或策略层加权，前者容易被视觉主导，后者要各训一个单模态策略。这篇把融合搬到低维动作空间：基座策略照旧只看视觉采样，触觉只在采样的后几步以梯度形式介入。对做 Agent 的人来说，它是一种不碰权重的推理时干预，代价与收益都好隔离。',
 scene='人手举着一把锁和一把钥匙，机器人先要把钥匙捏住，再插进锁孔转开。钥匙每次被捏住的位置都略有不同，顶视相机看不出这点偏差；只看画面的策略会按训练时的角度直接往里插，钥匙一歪就被别出夹爪。削黄瓜也一样，刀刃悬在皮上半毫米，画面上同样看不出来。',
 recall_module='改的是采样过程，不是策略。基座策略的权重、观测接口与动作块长度都保持原样，它照旧只看视觉产生一条粗动作；被换掉的只是最后几个采样步里速度或噪声的取值，多出一项来自触觉的梯度。它没有给系统加新的决策层，没有引入规划器，也没有把触觉变成策略的一路输入——论文自己的对照恰好说明后者会把 π₀.₅ 拖低。它同样没有改动谁来宣布任务完成，评测仍按人工设定的判据与时限算。',
 recall_io='输入有三样进 CPM：当前视觉图、当前触觉观测，以及这一步还带着噪声的动作块。触觉有两种形式可选，一种是 Xense 传感器直接给的触觉图，一种是 XenseSDK 在板上算出的 35×20 三维力场，两者在算法里除维度外处理方式相同。视觉与触觉都过同一个冻结的 DINOv2，动作过 1D CNN 加 MLP，三者汇成一个标量可行性分数。输出是这个分数对动作的梯度，按时间系数缩放后减在基座预测的噪声或速度上，采样继续往下走，最终交出一整块动作去执行。终止条件只有一个：采样步数走完。这篇没有失败检测，也没有恢复通路——CPM 判的是当前动作与当前接触状态合不合得上，不是上一次动作有没有做成；分数低只会让这一步的梯度更大，不会触发重试、重新定位或换一条计划。真正的纠错窗口只有下一次采样时更新的那帧触觉观测。',
 steps=[('先固定基座策略，只在采样过程里插手', '基座是两个现成实现：Diffusion Policy 走 RoboFactory 的复现，π₀.₅ 用官方实现与 pi05_base 权重加 LoRA。两者都在自采数据上训过，引导阶段权重一律不动。引导的动作全发生在采样循环内部：在预测的噪声或速度上减一项梯度，伪代码多出四行。'), ('CPM：把观测与动作压到同一个球面上求内积', '接触物理模型用冻结 DINOv2 同时编码视觉图与触觉图，经 N 层 Transformer 融合成潜观测；动作用 1D CNN 加 MLP 编码。两者 L2 归一化后取内积，即可行性分数。附录 G 给出概率解释：在潜空间高斯近似下，这个内积的梯度正比于把动作推向真实动作的方向。'), ('训练：对比学习加噪声预训练', 'CPM 由作者自训，只用少量专家演示，正样本是同一时刻的观测与动作，负样本取自错开的时刻，温度可学。关键的一步是噪声预训练：推理时送进 CPM 的动作是带噪的，所以训练时也按同一个调度器加噪，加噪步数从几何分布采样，流匹配模型改用线性插值。'), ('引导只在后期开，超参按任务重调', 'Diffusion Policy 用 100 步 DDPM，最后 10 到 20 步施加引导，尺度 3 到 4；π₀.₅ 走 10 步流匹配，引导窗口 0.2 到 0.3，即最后两到三步，尺度取 10。终止条件是采样步数走完，没有重试也没有外层循环。超参要按任务与策略重调。'), ('数据来自自研的手持采集器 TacUMI', 'TacUMI 是 UMI 式手持夹爪，用 Vive Tracker 加两个 Lighthouse 定位，基础成本 720 美元、整机 540 克；刚性指尖让操作者直接摸到力，夹爪开度由磁编码器读出而非视觉标记，板载算力跑 XenseSDK 在线把触觉图转成 30 Hz 力场。')],
 example='教学推演：回到上面的场景。第一步，π₀.₅ 照旧只看图像和指令，它的动作专家从噪声出发跑 10 步流匹配，前七步完全没有触觉参与，先得到一条视觉上说得通的粗动作，大致朝锁孔伸过去。第二步，从第八步起触觉介入：CPM 用冻结的 DINOv2 分别编码当前视觉图与指尖触觉图，经 Transformer 融合成潜观测，与动作编码器给出的潜动作各自归一化后取内积，得到一个标量可行性分数。这个分数衡量的是这段动作与此刻的接触状态合不合得上，而不是它离目标还有多远。第三步，对动作求梯度，按 k/(1−k) 缩放再乘引导尺度 10，从速度场里减掉，采样继续往下走。钥匙被捏偏的那一点差别在图像上看不出来，却会改变指尖的力分布，于是梯度把插入角度往偏差的反方向推。第四步，10 步走完，动作块直接执行，没有重试也没有外层循环，下一次机会要等下一轮采样读到新的触觉观测。这套机制的边界也在这里：它只能让每一次动作生成得更合理，一旦钥匙已经被别出夹爪，CPM 只会给所有候选动作打低分，却没有任何模块会说这一次失败了、要退回去重来。作者在局限里也把任务专属训练列了出来——换一项任务，这个 CPM 要从头训。',
 code='# 基座权重全程不动；CPM 由作者用少量演示对比学习训出\nA = 采样初值()                       # 高斯噪声\nfor k in 采样步(DP 100 步 / π₀.₅ 10 步):\n    e = base_policy(A, V_t)          # DP 出噪声，π₀.₅ 出速度，都只看视觉\n    if k <= K_TouchGuide:            # DP 取最后 10~20 步；π₀.₅ 取 0.2~0.3\n        s = CPM(V_t, T_t, A)         # 冻结 DINOv2 编码视触 → 与动作取内积\n        g = 梯度(s, A)\n        e = e - η * 时间系数(k) * g   # DP 用 √(1−ᾱ)，流匹配用 k/(1−k)\n    A = 更新(A, e)\n执行(A)                              # 终止条件只有采样步数走完\n# 没有成功判定也没有重试：下一轮靠新的触觉观测重新采样',
 setup='五项真机接触任务：系鞋带 100 条演示、薯片交接 50、黄瓜削皮 50、花瓶擦拭 30、开锁 20。前三项用 Bi-ARX5 双臂，后两项用 Flexiv Rizon4 单臂配 TacUMI。每个方法 20 次，初始条件适度随机；系鞋带、交接、开锁记成功率，削皮与擦拭按人类专家完成量的比例打分。',
 headers=['方法 · 表 I', '薯片交接', '黄瓜削皮', '开锁', '五任务平均'],
 rows=[['Diffusion Policy', '5%', '0.500', '0%', '16.3%'], ['TouchGuide · DP · 触觉图', '25%', '0.810', '20%', '36.2%'], ['π₀.₅', '25%', '0.785', '20%', '35.9%'], ['π₀.₅ 加触觉观测拼接', '15%', '0.760', '10%', '30.3%'], ['TouchGuide · π₀.₅ · 触觉图', '60%', '0.975', '30%', '58.0%']],
 findings='两个基座的绝对水平差得远，不能把 58.0% 当成这套方法的固有成绩，换成 Diffusion Policy 只有 36.2%，系鞋带那一列所有 DP 系方法都是 0%。另一处容易误读的是把触觉当额外一路观测拼进 π₀.₅ 反而掉到 30.3%，低于不给触觉的 35.9%，说明收益来自引导这种介入方式，而不是触觉这条输入本身。',
 ablation='表 II 显示噪声预训练是成败关键，去掉后三任务平均从 62.50% 掉到 39.17%。表 III 拆输入模态，只去视觉 43.33%、只去触觉 43.50%，两边几乎一样重。附录的编码器消融里，两个从零训的 ResNet18 只有 49.0%，不如冻结的 DINOv2。',
 boundary='模型来源分三层。编码器是现成的冻结 DINOv2，视觉与触觉共用一套；基座策略是现成实现与现成权重，Diffusion Policy 取 RoboFactory 的复现，π₀.₅ 用官方 pi05_base 加 LoRA，但两者都由作者在自采数据上训过 2 万到 4 万步，引导阶段才冻住；CPM 完全由作者自训，论文自己在局限里写明它是任务专属的，换一项任务要重新训一遍，这是最实在的前提成本。采集侧还要算上 TacUMI 这套硬件：Vive Tracker 与两个 Lighthouse、磁编码器、板载算力与 Xense 传感器，基础成本 720 美元且需要布置基站。所有结论都来自 20 次试验的真机评测，一格差一次就是 5 个百分点，系鞋带一列多数方法为 0，靠它区分方法强弱并不可靠。推理开销不大，π₀.₅ 从 18.52 fps 降到 17.24 fps；但整套机制里没有任何执行之后的判定环节。',
 transfer='这是推理时引导这一类的典型：与 Harness VLA 同样不动策略权重，但那边靠外层 Agent 换起步状态，这边直接改采样梯度，时间尺度差了几个量级。',
 exercise='取一个扩散策略，在采样后段接一个打分器，扫引导尺度与引导步数，同时记录被引导前后动作的差异大小。',
 answer='有解释力的证据是分数与真实结果相关：打分高的动作确实更常成功，且尺度调过头时成功率下降。只报一个最优点无法排除它在拟合超参。',
 sources=[('方法：§IV', 'S4'), ('接触物理模型：§IV-B', 'S4.SS2'), ('主结果：表 I', 'S5.T1'), ('消融：表 II', 'S5.T2')])

add('at-vla', version='v2',
 takeaway='触觉不是一直有用：用一个学出来的门决定何时注入，并让它走一条比视觉语言快三倍的通路。',
 result='四项真机接触任务各 15 次，整任务平均从 GO-1 的 0.22 升到 0.50；同样的触觉若全程注入反而掉到 0.13。',
 why='给预训练 VLA 加一路没见过的模态，通常做法是下游微调时直接拼进去，但这会挪动模型原有的注意力，抓取这种本来做得好的阶段反而变差。这篇提出两个问题：什么时候该让触觉说话，以及在 VLA 本身推理很慢的前提下，高频触觉怎么才来得及影响动作。',
 scene='机器人要顺着一条弯曲的拉链把袋子拉开，再换只手去拧瓶盖。自由空间里移动的时候，指尖什么都没碰到，触觉读数是一片平的噪声；真到拉链发紧、瓶盖打滑的瞬间，读数才变得有意义。把这条输入从头接到尾，模型连该抓哪个物体都开始抓偏。',
 recall_module='改的是预训练 VLA 内部动作专家的条件输入：加一个判断有没有接触的门，门开时把交叉注意力的 query 从状态 token 换成触觉 token，并让触觉以三倍频率单独走一条通路。它没有改视觉语言主干，InternVL-2B 那一路照旧；没有追加新 token 或改变特征维度，这正是它与常见做法的分界；也没有改动作的输出形式，仍是 14 维双臂末端位姿的动作块。更没有加高层规划或执行之后的验收。',
 recall_io='输入是三路图像（前视加左右腕）、一句语言指令、本体状态，以及触觉。触觉默认取 Xense 传感器的 6 维合力，三维法向加三维切向；另两种可选格式是 2 维 marker 位移与视触图像，后者用现成的 Sparsh 编码。这些输入被分到两条频率不同的流上：图像与语言进慢流，由 InternVL-2B 产出潜特征，在一个动作块窗口内被重复使用；触觉进快流，每次都取最新的一帧。输出是 14 维的双臂末端位姿动作块，由 DiT 动作专家生成，推理时快慢比固定 3:1，单次闭环 0.04 秒。失败这条通路论文没有：门控网络判的是此刻有没有接触，不是上一步做没做成，它的监督标签也只有接触与非接触两类。系统里没有成功检测器、没有重试、没有重新规划，所谓对失败的应对只是触觉读数变化之后下一次动作被改写——拉链发紧时靠力的变化把轨迹拉回来。论文报告的失败例子里，拧盖时夹爪打滑就没有被任何模块识别出来。',
 steps=[('先认一件事：直接拼触觉会把预训练能力拽坏', '作者做了个对照，一边只微调动作专家、不给触觉，一边把触觉编码器的输出全程当条件送进去。后者不但没变好，抓取定位还明显变差；动作专家的注意力图显示焦点从目标物体挪到了周边区域。消融表里这一格是 0.13，比原版 GO-1 的 0.22 还低。'), ('触觉门：学一个二分类判断此刻有没有接触', '触觉编码器是几层 MLP 的轻量模块，输出触觉 token；门控网络同样是 MLP，读这个 token 给出一个接触分数，超过阈值就打开，论文举的例子是 0.5。监督靠人工把训练轨迹逐帧标成接触与非接触，用二元交叉熵，与动作损失按 0.01 的权重一起训。'), ('自适应交叉注意力：换 query，不换结构', '动作专家原本用图文 token 作 key 和 value、状态 token 作 query。门关着时与原版 VLA 完全一致；门开了就把 query 换成触觉 token。这样不追加新 token、不改特征维度，避免扰动预训练模型对 token 序列的建模。'), ('双流：慢的看图说话，快的摸', '视觉与语言走慢流，由 InternVL-2B 那一路低频产出潜特征，作为一个动作块窗口内的条件；触觉走快流，高频喂进动作专家。每次预测都用最新的触觉加上最近一次的视觉语言输出。训练时快慢比在 1 到动作块长度之间随机取，推理固定 3:1，闭环时延 0.04 秒。门关着时两流同频。'), ('触觉格式与训练成本', '默认格式是 Xense 传感器的 6 维合力，三维法向加三维切向；另两种是 2 维 marker 位移与视触图像，后者借现成的 Sparsh 预训练编码器。作者训练触觉编码器、门控网络与下游微调的动作专家，基座 GO-1 的视觉语言主干沿用 AgiBot World 预训练权重。')],
 example='教学推演：回到上面的场景。第一步，机械臂还在自由空间里朝拉链头移动，指尖没有接触，触觉编码器给出的 token 送进门控网络后分数低于阈值，门关着。此时动作专家的交叉注意力与原版 GO-1 完全一致：图文 token 作 key 和 value，状态 token 作 query，两条流同频。抓取靠的是 AgiBot World 上预训练来的视觉定位能力，作者的对照说明这一段最怕被打扰。第二步，夹爪咬住拉链头，法向力跳起来，门被打开。query 从状态 token 换成触觉 token，序列长度与特征维度都没变，模型不必面对一段它从没见过的 token 排布。第三步，双流拉开频率：InternVL-2B 那一路按动作块窗口低频更新一次潜特征，触觉那一路连续跑三次，每次都拿最新的一帧力读数生成动作，单次闭环 0.04 秒。拉链走到弯处开始发紧时，切向力先变，轨迹在下一个 0.04 秒里就被拉回曲线上。第四步，拉完松手，接触消失，门关回去，模型退回原版 VLA 的行为去做下一段。整条链路里没有人问过这次拉开了没有：门只回答有没有碰到，不回答做没做成。论文自己报告的拧盖失败正是这样溜过去的——夹爪握得不够紧、拧到一半打滑，没有任何模块把它识别成一次失败。',
 code='# 基座 GO-1（InternVL-2B + DiT 动作专家）；触觉编码器与门控网络由作者训练\nz_T = 触觉编码器(force6D)            # 几层 MLP；Xense 给三维法向 + 三维切向\nif 门控网络(z_T) <= 0.5:             # 门关：与原版 VLA 完全一致\n    kv, q = 慢流(I, L), z_S          # 图文作 key/value，状态 token 作 query\n    执行(动作专家(q, kv))            # 两流同频，14 维双臂末端位姿动作块\nelse:                                # 门开：只换 query，不加 token、不改维度\n    kv = 慢流(I, L)                  # 低频，一个动作块窗口内复用\n    for _ in range(3):               # 快慢 3:1，单次闭环 0.04 秒\n        z_T = 触觉编码器(最新一帧触觉)\n        执行(动作专家(q=z_T, kv=kv))\n# 终止条件是动作块跑完、接触消失后门自己关回去\n# 门只判有没有碰到，不判做没做成；没有成功检测，也没有重试通路',
 setup='平台是 AgiBot Genie1，双 7 自由度臂，一台前视相机加两台腕部相机，夹爪装 Xense 触觉传感器，演示由 VR 头显遥操作采集。每项任务 30 到 50 条演示，测试 15 次。接触密集任务四项：拉开拉链、盖章、擦花瓶、拧盖；另有取放与开抽屉两项非接触任务。',
 headers=['整任务成功率 · 表 1', 'GO-1', 'π₀.₅', 'AT-VLA'],
 rows=[['拉开拉链', '0.20', '0.0', '0.33'], ['盖章', '0.13', '0.20', '0.46'], ['擦花瓶', '0.07', '0.33', '0.67'], ['拧盖', '0.27', '0.46', '0.53']],
 findings='分母是 15 次，0.07 就是一次，四项任务的差距多在两到八次之间。表 1 里 VTLA 与 RDP 那两行不能与前三行并排读：它们没有预训练权重，只在接触阶段的子集上训练，测试时由人工把机器人摆到理想起始构型，所以拧盖 0.80 与 0.53 比的不是同一件事，论文自己也说明了这一点。',
 ablation='表 3 按四项接触任务取平均：原版 GO-1 是 0.22，全程直接注入 6 维力掉到 0.13，加上触觉门与自适应交叉注意力升到 0.39，再加双流到 0.50。换触觉格式结论一致，marker 2 维从 0.05 到 0.32，视触图像从 0.02 到 0.40。三种格式里维度最低的 6 维力最好。',
 boundary='模型来源分得比较清楚。基座是现成的 GO-1，用 InternVL-2B 作视觉语言模型、DiT 作动作专家，在 AgiBot World 上预训练；视触图像那一路的触觉编码器借的是现成的 Sparsh。作者自己训的是轻量触觉编码器、门控网络与下游微调的动作专家，自适应交叉注意力则是对原结构的改动。前提成本有两块：一是每项任务 30 到 50 条遥操作演示，二是门控所需的逐帧接触标注，这份标注由人工完成，论文没有给出标注工时或一致性检查。评测全部在同一台 Genie1 上、每项 15 次，没有跨本体验证；训练步数、动作块长度的取值、门阈值除举例的 0.5 之外是否调过，论文都没有给出。还有一处要留意：所谓模态无关只在两项非接触任务与盖章上验证过，掉了触觉之后盖章从 0.46 回到 0.20。',
 transfer='与 TouchGuide 放一起读最清楚：那边完全不碰权重、只改采样，这边改的是架构与训练；对比 Harness VLA，决定何时交权的从外层 Agent 变成一个学出来的门。',
 exercise='把接触门换成固定阈值的力大小判据与一个永远打开的门，比较三者在自由空间段与接触段各自的失败次数。',
 answer='要看的是门的错误代价是否不对称：过早打开会伤到抓取定位，过晚打开只损失反应速度。只报总成功率看不出这两种错哪一种在起作用。',
 sources=[('自适应触觉注入：§3.2', 'S3.SS2'), ('反应双流：§3.3', 'S3.SS3'), ('主结果：表 1', 'S4.T1'), ('消融：表 3', 'S4.T3')])

add('vital', version='v1',
 takeaway='不重训策略：先用视觉在候选里选模式，再用触觉把选中的那段动作改写。',
 result='三个真机接触任务各 20 次试验，纯视觉基础策略总体成功 30%/30%/40%，同一条策略经引导升至 90%/75%/80%（附录 C.2）。',
 why='如果你已经在用“采样多条候选、让验证器挑一条”的做法，这篇要补的是验证器看不见的那一半。视觉能判断机器人是不是朝正确的杯子去，却判断不了夹得稳不稳、挤压力有没有过头，而这类接触量决定接触密集任务成不成。',
 scene='桌上摆着红、黄、蓝三只杯子，指令是把红杯里的液体移到蓝杯、再把滴管放回去。一条只看图像训出来的扩散策略，可能朝黄杯走，也可能方向对但半路把液体挤了出来。这两种失败在第三人称和腕部画面里都不明显：画面上滴管一直被夹着，看不出夹得太松还是太狠。',
 recall_module='改的是推理时的采样与去噪这一层。基础策略权重一次都不动，不收新演示，不扩动作空间，两个观测编码器全程冻结，动作仍然来自那条扩散策略的分布。新增的是三样东西：一个建在冻结编码器之上的潜空间视触动力学模型，把候选动作展开成可评的未来；一对语言条件验证器，视觉那侧直接用现成模型，触觉那侧是作者提出的、在对齐潜空间里算文本与触觉嵌入余弦相似度的奖励；以及把两者串起来的双层优化——视觉在长视野上选模式，触觉在短视野上改写，而不是把两个奖励加在一起。',
 recall_io='输入是当前两路 RGB、一张 GelSight 触觉图、本体状态，以及一条被视觉语言模型离线拆成阶段性视觉目标与触觉目标的任务文本。输出是一段 8 步动作块，直接交给机器人执行；产生它的过程分两步——先从基础策略采 10 条 16 步候选，在世界模型里滚出视觉潜变量、解码成图像交给 ROBOMETER 排序，选出视觉锚；再把锚的前 8 步加噪到第 4 级，用触觉奖励对干净动作估计的梯度改写噪声预测，逐级去噪出最终动作。失败没有独立的处理通路：系统不做成功检测，也没有重试、回退或重新摆位这类动作，所谓的修正完全发生在执行之前——每执行 8 步就用新的真实观测重新采样、重新排序一次，上一块动作做砸了只能靠新观测在下一轮被间接反映。阶段切换同样是单向的，视觉奖励越过 0.70 就换目标，论文没有给出越过之后发现判断有误时退回的机制。',
 steps=[('基础策略与两个冻结编码器', '基础策略是作者自训的扩散策略，Diffusion UNet 加 ResNet-18 编码器，每任务 50 条演示，出 16 步动作块、执行 8 步，主实验用纯视觉版。观测编码器不训：视觉冻结 DINOv3，触觉冻结 AnyTouch2，触觉图来自指尖 GelSight Mini。'), ('潜空间视触世界模型：作者训练的那一块', '在两个冻结编码器之上，作者训了一个 Transformer 潜空间动力学模型，按动作块递推预测视觉与触觉潜变量，另训两个解码器把潜变量还原成图像。数据是 50 条演示加约 250 条不同检查点的策略回放。长视野靠递归：世界模型出一段潜变量，解码后再喂回策略要下一块动作。'), ('视觉这一层：采 10 条候选，用现成奖励模型排序', '每次策略查询采 10 条 16 步候选，各自在世界模型里滚出来，把末帧潜变量解码成 224×224 图像，送进现成的 ROBOMETER-4B 做零样本打分，取分最高的那条当视觉锚。作者只为在线评测加了 KV 缓存与滑窗上下文，不微调，也不对这个视觉语言模型求梯度。'), ('触觉这一层：把锚动作部分加噪再引导去噪', '取视觉锚的前 8 步，按 SDEdit 的做法加噪到第 4 级，再用基础策略反向去噪。每一级先还原干净动作估计，送世界模型的触觉分支预测触觉潜变量，用它与阶段性触觉文本的余弦相似度当奖励，对动作求梯度、单位化后以系数 10 改写噪声预测。'), ('阶段切换与循环怎么停', '任务目标由一个视觉语言模型离线拆成逐阶段的视觉与触觉文本，这个模型的型号论文没有给出。移液任务分两段：最高视觉奖励越过 0.70 就切到第二段目标，触觉文本同时从轻夹换成重夹并保持 40 步。执行完 8 步就重新观测、重新采样，循环里没有成功检测，也没有回退。')],
 example='教学推演：回到上面的场景。第一步，动作来自作者自训的那条扩散策略，它只看两路 RGB，权重全程不动；观测先经冻结的 DINOv3 与 AnyTouch2 编成潜变量。第二步，一次查询采 10 条 16 步候选，每条都在潜空间世界模型里递推展开，把末帧解码成图像。第三步，ROBOMETER-4B 按当前阶段的文本目标给这十张图打分，“把液体移到蓝杯”这句话让朝黄杯去的候选拿到低分，方向问题在动作出手之前就被排掉，选中的那条成为视觉锚。第四步，锚的前 8 步被加噪到第 4 级再去噪：每一级先还原出干净动作估计，送进世界模型的触觉分支预测触觉潜变量，与“轻轻夹住”算余弦相似度，梯度单位化后按系数 10 改写噪声预测，于是夹得过狠、会把液体挤出来的那类动作被压下去。第五步，视觉奖励越过 0.70 后阶段切换，触觉目标换成用力夹住并保持 40 步，视觉目标换成把滴管放回红杯；执行完这 8 步就重新观测、重新采样。要点明这套机制没做到什么：它没有成功检测，液体真的洒了没有任何模块会说出来；修正全部发生在执行之前，做砸的那一块只能等下一轮靠新观测间接纠正；阶段切换是单向的，论文没有给出判断有误时退回上一阶段的办法。',
 code='z_v, z_t = DINOv3(o_rgb), AnyTouch2(o_tac)      # 两个编码器全程冻结\ncands = [pi_theta(o) for _ in range(10)]         # 基础策略权重不动，采 10 条 16 步候选\nfor a in cands:\n    z_pred = world_model(z_v, z_t, a)            # 作者自训的潜空间动力学，递归两块\n    score[a] = ROBOMETER_4B(decode(z_pred[-1]), l_vis[phase])   # 现成模型，零样本\nanchor = argmax(score)[:8]                       # 视觉锚只定“去哪”\nx = renoise(anchor, K=4)                         # SDEdit 式部分加噪\nfor k in range(4, 0, -1):\n    a0 = clean_estimate(x, k)\n    r  = cos(world_model.tactile(z_v, z_t, a0), CLIP_text(l_tac[phase]))\n    x  = ddim_step(x, eps_theta(x, k) - 10 * sqrt(1 - alpha[k]) * unit(grad(r, a0)))\nexecute(x)                                       # 执行 8 步，再用新观测重来一轮\nif max(score) > 0.70: phase += 1                 # 单向切换：无成功检测，无回退',
 setup='三个真机接触密集任务：白板擦写、间隙约 1 毫米的插孔、移液转运。平台是 Franka Emika 加 GelSight Mini 与两路 RGB。每任务 20 次试验、两条指令各 10 次；成功拆成视觉成功与接触成功，两者都满足才算整体成功。奖励排序精度另按 40 对回放统计。',
 headers=['任务 · 附录 C.2（图 8）', '纯视觉基础策略', '加 ViTaL 引导'],
 rows=[['擦写白板', '30%', '90%'], ['移液转运', '30%', '75%'], ['插孔装配', '40%', '80%'], ['视触基础策略 · 上述三任务', '30% / 70% / 60%', '70% / 80% / 70%']],
 findings='这组数字取自附录 C.2 的图 8，与正文那句提升 51% 不是同一个口径，不要混着引。每任务只有 20 次，一次试验就是 5 个百分点，插孔从 40% 到 80% 只对应 8 次差别。另外基础策略更强不等于引导后更强：视触策略在擦写与插孔上反而不如纯视觉策略被引导的结果。',
 ablation='表 1 用偏好序精度衡量验证器：视觉在真值观测上 83.3%、在预测未来上 85.0%，触觉 78.3% 与 81.7%；插孔的视觉只有 70%，作者归因于 ROBOMETER 分不清左右。附录 C.1 显示联合建模在 FID 与光流误差上好过单模态。表 13 的单步耗时 471 毫秒，基础策略 3 毫秒。',
 boundary='模型来源要分两层算。冻结的现成件：视觉编码器 DINOv3、触觉编码器 AnyTouch2、文本侧的 CLIP 文本编码器、视觉验证器 ROBOMETER-4B 零样本使用；把高层指令拆成阶段目标的那个视觉语言模型，论文没有给出型号。作者训练的：每个任务一条扩散策略、一个潜空间动力学模型与两个解码器，都依赖该任务的 50 条演示与约 250 条回放。因此不重训策略只对基础策略权重成立，换一个任务整套世界模型要重训，这才是真正的前提成本。规模上只有三个真机任务、每任务 20 次，摘要里的 51%、33%、20% 都是相对提升，绝对成功率要回附录 C.2 查。作者自述的限制也要带上：整套方法依赖世界模型的保真度，长程递推误差会累积到验证；触觉编码器的预训练数据远小于现代视觉语言模型。还有一处细节对不上：正文说世界模型用 250 条回放，擦写任务的附录写的是 280 条。',
 transfer='与 Harness VLA 同构：都不动策略权重，只改它被调用的方式。区别在于 Harness 用解析原语把机器人重新摆位，这篇直接在动作分布内部改写。',
 exercise='固定基础策略与总采样预算，比较只按视觉排序、只按触觉引导、两个奖励相加、以及先选后改四种做法。',
 answer='关键是把预算对齐：若先选后改在同等采样数下仍更好，才支持分层的说法。同时分开记视觉成功与接触成功，否则看不出是哪一层在起作用。',
 sources=[('双层引导与验证器：§4', 'S4'), ('触觉奖励定义：§4.3', 'S4.SS3'), ('主结果：图 2', 'S5.F2'), ('不同基础策略：附录 C.2', 'A3.SS2')])

add('taco', version='v2',
 takeaway='用触觉世界模型在失败点附近想象修正段，再把它当训练数据喂回策略。',
 result='六任务各 40 回合：基础 π₀.₅ 平均任务分 0.375，经两轮后训练到 0.825，同期最强基线 DreamGen 0.550（表 I）。',
 why='接触失败在 RGB 上几乎看不出来：擦不掉的板、拧不开的盖，画面里都像做对了。人工补修正演示要有人守着一次次接管，做到六个任务就做不下去。这篇要回答的是在哪里找失败、修正数据从哪来，以及怎么让触觉监督不把预训练的视觉语言表示改坏。',
 scene='机械臂拿着橡皮擦覆盖住白板上的星形，却始终没压出足够的力，星形一直在。另一段回合里它拧瓶盖，指尖贴住盖面却一直打滑，扭矩没传上去。这两段的前视画面几乎和成功回合一样，靠人守在旁边一次次接管来补修正数据，六个任务铺开就撑不住。',
 recall_module='改的是数据来源与后训练配方这一层。任务目标、成功判据、机器人平台与评测协议都不动，策略结构也不换，仍是那个 π₀.₅ 检查点。没有改的是执行期：策略部署时照样开环吐动作块，没有加在线的成功检测、重试或恢复。真正新增的是训练前的一条流水线——用带符号进度在失败回合里定位失败点，用视触联合生成在那个点上想象若干条局部修正，用同一个价值模型给这些想象段贴动作与进度标签，过滤排序后并进训练集；再配一套冻住 VLM 主干的后训练，让触觉监督不去改写预训练的视觉语言表示。',
 recall_io='要分两条通路看。训练侧的输入是当前策略跑出来的真实回合、原始演示，以及人工标注的子任务关键帧；输出是一份累积的修正数据集和更新后的策略权重。执行侧的输入是前视 RGB、两枚指尖的 12 维力矩与本体状态，输出是 30 步动作块，推理时用无分类器引导把速度场偏向正优势那一侧。失败怎么被处理，得说清它发生在哪一层：执行期没有任何失败通路，策略不判自己有没有做成，也不会在回合内重试；失败要等这一整回合跑完、进入下一轮修正循环才被看见。看见的方式是价值模型沿回合估一条带符号进度，在进度停滞或下降的那个短窗标出锚点，再在那里想象修正。换句话说，恢复不发生在机器人身上，而发生在下一次训练里；同一次失败在当轮无法挽回，而如果价值模型把失败点标错，错误会以正优势样本的形式进入训练集，运动学与力矩过滤查不出这种错。',
 steps=[('先把底座说清楚：哪些是现成的', '所有方法共用一个起点：公开的 π₀.₅-DROID 检查点，PaliGemma 2B 主干加 300M 动作专家，在每任务 50 条演示上微调。视触生成模型从 Wan2.2-TI2V-5B 起步，先在 DROID、AgiBot、RoboMIND 与闭源数据上续训，再全参微调。'), ('Recognize：靠带符号进度找失败点', '逆动力学与价值模型由作者训练，视觉侧是 DINOv2-with-Registers 加方向感知解码器，触觉侧是 MLP 吃 12 维力矩。进度标签由人工标注的子任务关键帧线性插值而来，失败回合从失败起点往后取负。只在失败回合里找锚点，条件是短窗内进度增量低于阈值，末端状态排除。'), ('Imagine：一个锚点生成 32 段视触修正', '生成模型以锚点帧、当时的力矩读数和任务文本为条件，联合去噪出 49 帧视频与同长度力矩序列。视频与力矩 token 拼在一起进 DiT 自注意力，用时间 RoPE 把每四个力矩 token 对到一个视频潜时刻，首帧力矩不加噪当锚。每个锚点用不同随机种子采 32 段。'), ('Label 与两道过滤', '同一个逆动力学与价值模型逐帧给想象段贴上动作与进度标签。过滤查两件事：按锚点构型重建的指令轨迹有没有连续逆运动学解且不越界，预测力矩的幅值与变化率在不在真实轨迹标定出的范围内。通过的按进度增益排序，最多留 8 段，标成正优势样本。'), ('知识隔离后训练，以及循环怎么停', '后训练冻住 VLM 主干，只更新触觉编码器、适配层与动作专家。演示与成功回合标正，失败回合以失败起点为界前正后负，留下的想象段标正；训练时以 0.1 概率把标签换成空，推理时用无分类器引导偏向正标签分布。循环固定跑两轮，每轮生成模型与价值模型一起更新，没有收敛判据。')],
 example='教学推演：回到上面的场景。第一步，先让当前策略在白板前跑完一整回合才谈修正，执行过程中没有任何模块会说力不够。第二步，逆动力学与价值模型沿这条回合逐帧估一个带符号进度分，橡皮擦贴上板面之后进度不再涨，短窗内的增量低于阈值，这一帧就被标成失败邻近状态；末端帧排除，成功回合不参与找锚。第三步，视触生成模型以这一帧的画面、当时的 12 维力矩和任务文本为条件，用 32 个不同种子各去噪出一段 49 帧视频加同长度力矩序列，画面里橡皮擦压下去、星形被擦掉，力矩曲线同时抬起来。第四步，同一个价值模型逐帧给这些想象段贴上动作与进度标签，接着过滤：按锚点构型重建的轨迹要有连续逆运动学解，预测力矩的幅值与变化率要落在真实轨迹标定的范围里，通过的按进度增益排序，最多留 8 段。第五步，这些段标成正优势样本并进训练集，冻住 VLM 主干、只更新触觉编码器与适配层和动作专家，推理时用无分类器引导偏向正标签；两轮之后停。要点明没做到什么：定位失败点、给修正动作、给价值排序都是同一个模型干的，它标错的地方过滤挡不住，因为过滤只查运动学与力矩范围；真正的外部检查只有回放通过率，而那只抽了每任务每设定 50 个候选。',
 code='policy = finetune(pi05_droid, demos_50)         # 现成检查点，每任务 50 条演示\nfor k in range(2):                              # 固定两轮，没有收敛判据\n    rollouts = deploy(policy)                   # 执行期不判成败，跑完整回合\n    for tau in [t for t in rollouts if failed(t)]:\n        p = IDVM.progress(tau)                  # 带符号进度，失败起点之后取负\n        anchors = [t for t in tau if p[t + delta] - p[t] < eps]\n        for t in anchors:\n            segs = [Gen(I[t], F[t], lang) for _ in range(32)]   # 49 帧视频 + 力矩\n            segs = [s for s in segs if ik_ok(s) and force_ok(s)]\n            keep = top8(segs, key=IDVM.gain)    # 同一个模型既打标又排序\n            corr += label(keep, y=1)\n    policy = cfg_rl(freeze_vlm(policy), demos + rollouts + corr)\n    Gen, IDVM = update(Gen), update(IDVM)       # 生成器与价值模型每轮一起变',
 setup='六个真机接触任务：插花、擦白板、拧瓶盖、敲木琴、烤面包、移汉诺塔环。平台是 Franka Research 3，前视 RealSense D455 与两枚 Xense 力矩指尖，SpaceMouse 遥操作采 50 条演示。每任务 40 回合、初始物位随机，报任务分与仅在成功回合上平均的完成步数。',
 headers=['方法 · 表 I 六任务平均', '任务分 TS', '完成步数 CS'],
 rows=[['基础策略 π₀.₅（50 条演示微调）', '0.375', '185.5'], ['RFT · 第 2 轮', '0.425', '164.5'], ['DreamGen · 第 2 轮', '0.550', '131.7'], ['TACO · 第 1 轮', '0.650', '131.2'], ['TACO · 第 2 轮', '0.825', '117.0']],
 findings='任务分带部分完成的分数，不是成功率，0.825 不能读成 82.5% 成功。完成步数只在成功回合上平均，成功回合少的时候这个数会偏乐观。另外基础策略在移汉诺塔环上只有 0.075，六任务平均被这一项压低，TACO 把它拉到 0.775，平均值的涨幅有相当一部分来自这一个任务。',
 ablation='表 II 最有说服力：只生成视频时回放通过率 32.0%、策略任务分 0.275；联合生成视触后升到 86.0% 与 0.650；生成器不变、只给价值模型加触觉输入，动作损失从 0.038 减半到 0.019，回放通过率 98.0%。表 III 去掉知识隔离掉到 0.475，是所有消融里最狠的一项。',
 boundary='模型来源分两层。现成的：策略底座是公开的 π₀.₅-DROID 检查点，生成模型的底座是 Wan2.2-TI2V-5B，视觉特征用 DINOv2-with-Registers。作者训练的：生成模型的续训与全参微调、逆动力学与价值模型，以及每一轮的策略更新——而且生成模型与价值模型每轮都跟着更新，所以两轮之间变的不只是策略。人工成本没有消失，只是换了位置：进度标签来自人工标注的子任务关键帧，力矩合理范围按任务标定，回放通过率也靠人工复核。判据的自洽风险要点名：找失败点、给修正动作、给价值排序全由同一个价值模型做，判错就直接污染训练集，而过滤只查运动学与力矩范围，查不出动作合法但方向错的那类。回放通过率是唯一的外部检查，却只抽每任务每设定 50 个候选。规模上六个任务、每任务 40 回合，数据配比实验只覆盖两个任务。锚点窗长、进度阈值与增益阈值的取值，以及每轮采多少真实回合，论文没有给出。',
 transfer='与 RoboClaw 对照最有价值：都让同一个模型既判失败又供训练数据。ASPIRE 把修复沉淀成可复用知识，这篇沉淀成权重，出错后更难回溯。',
 exercise='把价值模型的失败点定位人为打偏若干帧，其余全部不变，看两轮之后的任务分怎么变。',
 answer='要同时记回放通过率与任务分。若定位打偏后过滤仍旧放行、任务分却下降，说明过滤只挡得住运动学错误，挡不住语义错误。',
 sources=[('方法三步：§III', 'S3'), ('主结果：表 I', 'S3.T1'), ('消融：§IV-C', 'S4.SS3'), ('训练配方：附录 B-C', 'A2.SS3')])

add('touchworld', version='v2',
 takeaway='把语义规划、触觉目标预测与高频残差修正拆到三个时间尺度上各自跑。',
 result='六任务各 100 次回放：干净设置平均 65.0%，人为扰动下 53.7%，比最强基线 FTP-1 高 15.7 与 18.5 个点（表 1）。',
 why='多数触觉策略把触觉当成又一路观测塞进同一个模型，于是慢的语义推理、中速的动作生成和快的接触反馈共用一套容量、挤在一个控制周期里。这篇的主张是把这三件事拆开跑，并让触觉同时承担两种角色：既是预测出来的接触参考，也是实时的反馈信号。',
 scene='指令是把电源插头插进插座。机器人已经对准插孔，推进的时候人从旁边轻轻碰了一下手腕，插头偏出两毫米就再也进不去。另一个场景里抽纸被拉出一半后打滑，画面上手指还贴着纸，纸却没再动，等下一块动作生成出来时早就来不及了。',
 recall_module='改的是策略的分层方式与运行节奏，而不是任何单个模型的能力。视觉语言主干与世界模型主干都是现成的开源权重，动作空间、任务集合与评测协议也都不变。没有改的是训练上的一体化：四个阶段各自用匹配自己时间尺度的监督分开训，不做跨层端到端反传，残差训练时标称策略是冻住的。新增的是三件事：一个带记忆、只输出可执行子任务的规划器，一个预测未来视触子目标的世界模型，以及一层只在触觉敏感子空间上改写标称动作的残差 Transformer。',
 recall_io='输入分三路进不同的层。规划器拿任务指令、当前画面与记忆，输出一条可执行子任务；世界模型拿指令、这条子任务、画面与触觉，输出一段 17 帧的视触子目标网格，只在相位变化时重算；标称策略拿上述全部加本体状态，输出 32 步动作块与一个上下文 token；残差层拿这个 token、16 步标称前瞻窗和近期触觉与本体历史，输出一个残差窗，加回标称动作后提交前 4 步。失败没有检测，也没有恢复通路。这套系统里的修正是连续的残差叠加，不是判定某一步失败再重试：残差层每 4 步用新的触觉读数刷新一次，接触偏了就在下一次刷新里被拉回来一点，没有任何模块会宣布这一步做砸了。规划器的记忆里确实存着一项执行状态，但这项状态由谁判定、判错之后有没有回退，论文没有给出。',
 steps=[('子任务规划器：现成 4B 模型加记忆，作者微调', '规划器从 Qwen3-VL-4B-Instruct 起步，用 LoRA 监督微调，训练集 128866 条，秩 16、跑 20 轮。输入是任务指令、当前画面与一份记忆，记忆里存过去的子任务、预测过的触觉子目标与执行状态；输出只有一条可执行子任务，自由形式的推理不进动作接口。'), ('触觉世界模型：先学人手，再学机器人', '世界模型从 Wan2.2-TI2V-5B 起步，先在 EgoTouch 人手交互视频上预训练，再用 LoRA 在 10 小时机器人演示（约 108 万帧）上微调。人手与机器人触觉布局不同，统一渲染成图像形式对齐；样本是 2×2 观测网格，预测 17 帧视触子目标。'), ('标称策略：把触觉画成图像，走同一条视觉语言通路', '标称策略是视触扩散 Transformer，用流匹配生成 32 步动作块。触觉不走单独编码器，而是渲染成图像与 RGB 一起进视觉语言分支；提示把原任务和当前子任务拼起来；世界模型的预测网格有就当额外目标上下文。动作 120 维。这条分支从哪个预训练权重初始化，论文没有给出。'), ('触觉残差 Transformer：全系统唯一的快通路', 'TRT 是 512 宽、8 层的小 Transformer，吃滑动的 16 步标称前瞻窗、近期触觉与本体历史，以及标称策略的上下文 token，输出残差窗加回标称动作。它只改 58 维触觉敏感子空间，其余维度照搬标称输出。训练时标称策略冻住，目标是演示高频动作与标称动作之差。'), ('三层各自的刷新节奏与终止', '高层按固定的慢语义速率更新，世界模型只在子任务或任务相位变化时才重算，稳定阶段复用上一份子目标。标称策略每 32 步出一块，TRT 在块内按偏移 0、4、8、12 刷新，每次只提交前 4 步。终止由规划器的相位判断驱动。真实反馈频率取决于控制频率与感知链路，论文没有给出赫兹。')],
 example='教学推演：回到上面的场景。第一步，子任务规划器看着当前画面和那份记忆，把插好电源插头这条指令切成当前该做的一段，比如对准插孔并推入；它只输出这一句，自由形式的推理不传给下层。第二步，触觉世界模型接过这句子任务，在 2×2 的观测网格上预测一段 17 帧的视触子目标，画出插到位时指尖压力该长什么样；这份预测只在相位变化时重算，插入过程中一直复用。第三步，标称视触策略把 RGB、渲染成图像的触觉、本体状态和拼好的任务加子任务提示一起吃进去，用流匹配吐出 32 步动作块和一个上下文 token。第四步，手腕被碰了一下，残差层在下一次刷新时看到近期触觉历史变了，在 16 步前瞻窗上算出一个残差，只改那 58 维触觉敏感维度，加回标称动作后提交 4 步，之后每 4 步再刷新一次。第五步，插入完成后规划器切到下一段子任务，世界模型这时才重新预测。要点明这套机制没做到什么：整个过程里没有任何模块判定这一步失败了，修正是连续叠加的残差，不是检测加重试；一旦碰偏到超出残差子空间能拉回的范围，系统不会退出来重新对准。规划器的记忆里确实有一项执行状态，但它由谁判定、判错之后有没有回退，论文没有给出。',
 code='while not done:\n    sub = planner_qwen3vl4b(task, image, memory)       # 慢语义层，作者 LoRA 微调\n    if planner_says_phase_changed(memory, sub):        # 相位没变就复用上一份子目标\n        goal = world_wan22(task, sub, image, tactile)  # 17 帧视触子目标\n    chunk, ctx = nominal(task, sub, goal, image, state, tactile)   # 流匹配，H=32\n    for off in [0, 4, 8, 12]:                          # 块内高频刷新\n        window = chunk[off:off + 16]\n        d = TRT(window, state_hist, tactile_hist, ctx) # 只改 58 维触觉敏感子空间\n        execute((window + d)[:4])                      # 提交 4 步，再用新触觉刷新\n    memory.append(sub, goal, status)                   # status 由谁判定，论文没有给出\n# 没有失败检测，没有重试：所谓修正就是残差叠加',
 setup='六个真机任务：浇花、桌面清理、放杯、插电源插头、擦锅、抽纸。平台是带灵巧手与 JQ 触觉手套的人形机器人，遥操作用 Meta Quest 头显加手套。每任务 200 条演示、100 次真机回放，每项都分干净设置与人为扰动设置，扰动包括移动目标、破坏接触与干扰抓握。',
 headers=['方法 · 表 1 六任务平均', '干净设置', '人为扰动'],
 rows=[['Pi-0.5', '40.7%', '27.7%'], ['FTP-1（单体触觉策略）', '49.3%', '35.2%'], ['GR00T N1.7', '39.3%', '26.0%'], ['TouchWorld', '65.0%', '53.7%'], ['TouchWorld · 插电源插头（最难一项）', '45%', '35%']],
 findings='两列对着看才有意义：扰动下所有方法都掉，按表 1 两列相减（论文未印出差值），TouchWorld 掉 11.3 个百分点，Pi-0.5 掉 13.0、FTP-1 掉 14.1。也别把 65.0% 当成整体可用：插电源插头在干净设置下只有 45%，最好的浇花也只有 72%。三个基线里只有 FTP-1 用触觉，和另外两个比更像在比有没有触觉。',
 ablation='消融只有图 5 的堆叠柱状图，正文没有给出任何数值，只说去掉触觉输入掉得最多、去掉残差层主要伤扰动设置。能核对数字的是另两张表：表 2 里世界模型的接触 IoU 是 52.7%，直接复制当前触觉只有 31.8%；表 3 里带记忆的 4B 规划器子任务准确率 88%，零样本 32B 是 69%。',
 boundary='模型来源分两层。现成底座：子任务规划器是 Qwen3-VL-4B-Instruct，世界模型是 Wan2.2-TI2V-5B，人手预训练用公开的 EgoTouch 数据。作者训练的：规划器的 LoRA 微调、世界模型在 10 小时机器人演示上的 LoRA 微调、标称视触策略的完整训练，以及残差 Transformer；标称策略那条视觉语言分支从哪个权重初始化，论文没有给出。四个阶段分开训、不做端到端反传，因此整体收益不能记到某一个模块头上。基线这一侧要留神：论文没有说 Pi-0.5、FTP-1 与 GR00T N1.7 有没有在同一批每任务 200 条演示上微调，也没有说除 FTP-1 外谁拿得到触觉输入，所以表 1 的差距里混着数据与模态两个变量。系统被称作高频，但真实反馈频率取决于控制频率与感知链路，论文没有给出赫兹。作者自述的限制包括六个任务不足以覆盖家务多样性、子目标只做短视野预测、换触觉传感器或手型仍需标定与少量适配数据。',
 transfer='与 EMERGE-Policy 对照：都做分层，但那边靠结构化证据推进，这边只靠相位切换。与 π₀.₅ 比，多出来的是预测性触觉目标和一层快反馈。',
 exercise='固定标称策略，只改残差层的提交间隔，在扰动设置下比较提交 1 步、4 步、8 步的成功率。',
 answer='要同时记单位时间里的残差刷新次数。若成功率随间隔缩短而升，才支持快反馈的说法；若不变，收益更可能来自残差这种参数化本身。',
 sources=[('三层结构与调度：§2', 'S2'), ('主结果：表 1', 'S4.T1'), ('世界模型预测精度：§4.5', 'S4.SS5'), ('实现细节：§8', 'S8')])

add('n0-vtla', version='v1',
 takeaway='触觉不作为观测送进上下文，而是被预测成一组潜在 token，再去条件化动作专家。',
 result='二十项仿真任务平均成功率 63.8%，最强基线 44.0%；九项真机任务成功率全部领先，均值 47.2% 对 29.4%。',
 why='前面的 VLA 只看相机、语言与本体状态，接触和滑移只能间接推断。这篇要理解的是触觉该以什么形式进入策略：它既不把触觉塞进视觉语言前缀，也不用当前读数直接引导去噪，而是让模型先估计接下来一块动作会造成的接触变化，再用这个估计条件化动作生成。',
 scene='插头对着插座往下压。视觉看上去已经对准，实际却顶在插座边缘。只靠相机的策略此刻察觉不到任何异常：画面里插头仍在该在的位置，于是它继续压下去，直到这次尝试结束。抓一只空塑料瓶让它立住也一样，夹到能提起来和夹住却不提起，在画面上几乎没有区别。',
 recall_module='改的是策略内部的条件来源：在感知与动作之间新开一条触觉通路，触觉不进视觉语言前缀，而是先差分、编码，再被蒸馏成一组潜在 token 直接条件化流匹配动作专家。没改的部分同样要记住——动作与状态仍是继承自 π₀.₅ 的 32 维容器，动作块长度、去噪步数、视觉语言入口都照旧，底层仍是逆运动学加关节指令，没有轨迹规划与碰撞检测；策略外面没有规划器、技能库或验证器，触觉编码器主干全程冻结，作者只训它后面那层线性投影。',
 recall_io='输入是多路相机图像、离散化后写进前缀的本体状态、一句语言指令，以及每根参与操作的手指的触觉图像；触觉图像先减去该回合开头那帧零接触基准，再经冻结编码器与线性投影变成 token，这些 token 只走预测器，既不进视觉语言前缀，也不直接进动作专家。输出是长 50 的动作块，以绝对末端位姿加夹爪通道表示，经逆运动学变成关节指令，一次请求出一整块，合同要求整块执行完再请求下一块。失败这条通路要明确写出来：部署时没有成功检测器，没有验证器，也没有外部触发的恢复动作。插入被挡住后抬臂重对准，是策略在新观测下采出来的行为，不是某个模块判定失败后调用的；ALTER 的成对进度模型能在曲线上看出掉落与回退，但它只在离线打标签时使用，部署提示恒取正标签，运行时并不在环。某个视角的触觉流缺失时，控制器把该视角当成自身基准，也就是当作没有接触，而不是报错。',
 steps=[('基座与动作容器都沿用现成件', '接的是已发布的 π₀.₅ 权重：PaliGemma 主干加流匹配动作专家。动作与状态统一进一个 32 维容器，每臂占 10 维，含 3 维位置、6 维 rot6d 旋转与 1 维夹爪，其余 12 维恒零，单双臂数据因此共存。作者又在自建的 NeoData 上预训练一轮。'), ('触觉先变成差分图，再变成十个 token', '策略从不看原始触觉帧。回合开头夹爪张开静止至少 0.5 秒，那一帧记为零接触基准；之后每帧在像素上减去基准得到接触差分图，交给冻结的 DINOv2 编码，后接可训练线性投影。每视角出 10 个 token：一个 class token 加 3×3 池化的 9 个空间 token。'), ('预测而不是反应：潜在触觉 token', '一个轻量预测器读这些差分 token 与上下文化的视觉语言前缀，蒸馏成 10 个潜在触觉 token z。目标是同一编码器作用在未来 50 步触觉变化上、按视角平均的 z*，损失为对称 InfoNCE 加粗差分场 L1。触觉不进前缀，z 接在动作后缀最前面，自己不出动作。'), ('三段式把新通路接上去', '每段从上一段的检查点继续。第一段冻住整个基座，只训预测器、触觉投影与重建头；第二段冻住触觉感知栈，屏蔽视觉语言前缀对动作查询的注意，逼专家只能从 z 读触觉；第三段解冻除触觉编码器主干外全部参数，只用动作损失联合训练。之后每任务再单独适配。'), ('推理：整块执行，另有一条离线改进通路', '一次前缀编码加十步 Euler 去噪出长 50 的动作块，绝对末端位姿经逆运动学落成关节指令；合同要求整块执行完再请求下一块，截断重规划会丢掉块尾的夹爪闭合命令。作者另用 ALTER 在固定部署语料上按阶段相对进度打二元优势标签重训策略。')],
 example='教学推演：回到上面的场景。第一步，接住这件事的不是一个全新模型，而是现成的 π₀.₅ 权重：主干读图像、指令与离散化状态，流匹配动作专家出长 50 的动作块，作者只是在自建的 NeoData 上把它重新预训练了一遍。第二步，触觉要先变成模型读得懂的东西——回合开头夹爪张开静止半秒，那一帧被记成零接触基准；插头顶到插座边缘时，当前帧减去基准得到一张接触差分图，经冻结的 DINOv2 与一个线性投影变成 10 个 token。第三步是这篇的分岔口：这 10 个 token 既不进视觉语言前缀，也不直接进动作专家，而是交给一个轻量预测器，连同上下文化的前缀蒸馏成潜在触觉 token z；z 的训练目标是 50 步之后的触觉变化，所以它估计的是接下来这块动作会造成什么接触，而不是刚才碰到了什么。第四步，z 被投影后接在动作后缀前面，动作 token 注意它，它自己不出动作；专家之所以会用它，是因为第二段训练专门屏蔽了视觉语言前缀，让降低动作损失的唯一通路经过 z。第五步，运行时一块动作要整块执行完再请求下一块。顶到边缘之后抬臂、重对准、再插一次，这个行为是策略在新观测下自己采出来的，系统里没有任何模块判定过这次插入失败；ALTER 的进度模型确实能在曲线上看出物体掉落，但它只在离线打标签时使用，部署时并不在环。',
 code='backbone = π0.5 已发布权重 (PaliGemma + 流匹配动作专家)\nf_enc    = DINOv2(冻结) + 可训练线性投影        # 换新传感器只训这层投影\n# Stage1 冻基座：z 对齐 z* = 各视角 f_enc(tac[τ+50] - tac[τ]) 的均值，InfoNCE + L1\n# Stage2 冻触觉栈：屏蔽视觉语言前缀，只训 latent→expert 投影与动作专家\n# Stage3 解冻除 f_enc 主干外全部参数，仅用动作损失联合训练\n\nreset(): tac0 = 本回合第一帧          # 零接触基准；某视角缺失则按无接触处理\nwhile 任务未完成:\n    g = f_enc(tac[τ] - tac0)          # 每视角 10 个 token，不进视觉语言前缀\n    z = predictor(g, prefix(图像, 指令, 状态))    # 10 个潜在触觉 token\n    A = flow_match(expert, prefix, z, steps=10)   # 长 50 的动作块\n    execute(A, 全部 50 步)            # 截断重规划会丢掉块尾的夹爪闭合命令\n    # 没有成功检测：抬臂重试是策略自己采出来的行为，不是被判定触发的',
 setup='九项 NeoReal 真机任务各有自己的试验次数预算，同时报成功率与 100 分制的阶段进度分；仿真侧是 UniVTAC 的八项加 NeoSim 的十二项，每个策略用 100 条演示训练，按模拟器的完成判据算成功率。下表取自表 1、表 2 与图 7。',
 headers=['设置 / 方法', 'π₀.₅', 'N₀-VTLA'],
 rows=[['UniVTAC 八项均值（表 1）', '41.4%', '83.1%'], ['NeoSim 十二项均值（表 2）', '45.8%', '50.8%'], ['其中八项双臂任务均值（表 2）', '34.3%', '39.4%'], ['NeoReal 九项成功率均值（图 7）', '29.4%', '47.2%'], ['NeoReal 九项进度分均值（图 7）', '42.3', '56.8']],
 findings='增益很不均匀：UniVTAC 八项从 41.4% 抬到 83.1%，NeoSim 十二项只从 45.8% 抬到 50.8%，其中双臂那八项两者都不到四成。真机九项 47.2% 的均值同样说明离可靠还远；进度分之所以要单独报，正因为不少长程任务的整任务成功率接近零。',
 ablation='这篇没有逐个组件的成功率消融，支撑设计的是表示层证据：第一段之后，潜在 token 在约 32 个候选里以 92.3% top-1 命中未来触觉目标，只用当前触觉编码检索是 57%，随机是 3.2%；候选扩到 128 时两者为 81% 与 40%。free-latent 变体只在文字里提到，没有成功率。',
 boundary='模型来源要分三层算。现成的部分是 π₀.₅ 的已发布权重与冻结的 DINOv2 触觉编码器主干，两者都不为这篇重新训练。自训的部分则很大：预测器、触觉投影、重建头、latent 到专家的投影全部新初始化，而第三段联合训练又把动作专家与视觉语言主干一起解冻，再加上在 NeoData 上从头走一轮多平台预训练，以及 ALTER 里那个基于 π₀.₅ 结构的成对进度模型，所以这不是在基座上挂一个轻量模块。数据与算力这层论文自己留了白：NeoData 的规模与条目构成推给了配套的数据报告，算力一节明确说省略硬件型号、卡数与吞吐，因此复现成本无法从这篇估算。评测口径还有一处没写清——表里的 π₀.₅ 一行是否也在 NeoData 上预训练过，原文只说在统一协议下内部复现，这直接影响能否把差距全部记到触觉通路头上。',
 transfer='与 RT-2 对照：那边把动作离散成 token 占用词表，这边把触觉压成连续潜在 token 只做条件；与 π₀.₅ 比，换的是条件来源而非训练配方。',
 exercise='不必重训大模型。取一条已有策略，分别喂当前触觉、未来触觉真值与全零，比较接触段与自由空间段的动作差异。',
 answer='若分歧只出现在接触段，说明触觉在自由空间里确实是噪声；若喂真值明显好过喂当前值，才支持该预测而不是该反应这一主张。',
 sources=[('潜在触觉 token：§2.2', 'S2.SS2'), ('三段式训练：§4.2', 'S4.SS2'), ('仿真结果：表 1–2', 'S5.T1'), ('表示分析：§5.5', 'S5.SS5')])

add('star', version='v2',
 takeaway='灵巧手的触觉多数时候是空的：先与视觉对齐，再只留被触发的 token 和一个全局 token。',
 result='四项真机灵巧任务各 20 次试验，平均成功率 61%、完成率 79%；同数据微调但不用这套配方的 π₀.₅ 是 44% 与 61%。',
 why='这篇问的是同一个问题的另一半：手上贴着几百个触点时，难处不是没有信号，而是信号绝大多数时刻为零。作者把稀疏拆成空间、时间、信息三种，分别用联合预训练、按接触门筛 token、预测多个未来时刻来对付。值得学的是这三件事各自针对哪一种稀疏。',
 scene='让机器人用拇指和食指把一只耳机翻转 180 度。指尖此刻在做什么，相机基本看不见——手指自己挡住了接触点，画面上翻到一半和翻过去几乎一样。可就算把几百个触点全接进策略也不解决问题：绝大多数时刻它们读数为零，模型很容易学会把这一路当成常数忽略掉。',
 recall_module='改的是触觉这一路输入怎样被表示与被监督，落点全在策略内部：触觉先画成图像、与视觉做掩码重建联合预训练，再按接触门只保留被触发的空间 token，另补一个每只手一个的全局 token，训练时额外预测五个未来时刻的法向力。没改的是策略骨架——π₀.₅ 的主干与动作专家、流匹配目标、长 50 的动作块、十步去噪一概照旧，只因为换成灵巧手才把动作投影层从 32 维改成 64 维重初始化。策略外面没有规划器、没有技能库，也没有任何运行时的验证环节。',
 recall_io='输入是三路 RGB，左右腕部各一路加头顶一路，两只手的触觉图像，离散化后写进语言提示的 64 维本体状态，以及一句人工标注的任务级指令；整条序列 1562 个 token，其中视觉 768、触觉 394、语言最多 350、动作后缀 50。输出是长 50 的动作块，内容是相对当前状态的手臂末端位姿与手指关节位置，由十步 Euler 积分从高斯噪声解出，直接下发执行。训练时另有一路未来法向力预测头，推理时不使用。失败这一条要明确写出来：这是一个端到端策略，执行之后没有成功检测，没有验证器，也没有重试逻辑。耳机没翻过来、书没抽出来，系统内部没有任何模块会发现，纠正只能寄望于下一块动作在新观测下自然改变。评测里的完成率是人按子任务清单事后统计的，不是策略自己读得到的信号。触觉传感器的噪声、漂移与缺失触点，论文把它们列进未来工作，当前没有处理。',
 steps=[('先造平台再采数据', '双臂轮式机器人，每只手 10 个驱动自由度、共 16 个，掌心与手指内侧贴 268 维压阻式触点，指缘没有覆盖。遥操作用骨架式手套，重定向用 DexPilot，作者关掉自动闭合、补上朝向约束。最后采到 200 小时、10576 条轨迹、65 个任务，69.5% 需多指协同。'), ('触觉画成图像，与视觉做掩码联合预训练', '触觉按 T-Dex 的做法表示成图像：268 个触点按手上的空间布局摆进一张 224×224 的图，没有触点处填零。这张图与对应腕部 RGB 成对送进掩码重建预训练，各遮 75% 的 patch，左右手共用一个编码器。只在触觉上预训练会让模型学会一律预测零。'), ('稀疏加全局：接触门只筛空间 token', '策略训练时每只手的触觉图被编码成 196 个空间 patch token，另加一个可学习的全局 token，两手合计 394 个。接触门只作用在空间 token 上，token 内任一触点超阈值就保留，否则屏蔽。全局 token 恒保留，且不许注意视觉、语言与动作，反过来可以。'), ('稀疏未来触觉预测作为辅助任务', '训练时另加一项监督：在长 50 的动作时域上取第 10、20、30、40、50 步共五帧，由前缀输出的触觉 token 经 MLP 预测未来法向力。同样五帧的密集版本取第 1 到 5 步，30 Hz 下只覆盖 0.17 秒，稀疏版摊到 1.67 秒。两者的差异归因论文没有拆开。'), ('基座是现成 π₀.₅，训练分两段', '骨架直接用 π₀.₅：PaliGemma 的 Gemma-2B 主干加 300M 动作专家。因为换成灵巧手，动作维度从 32 改成 64、实际占 34 维，动作投影层被丢弃重初始化。先在 200 小时数据上微调 6 轮，再对每项任务用 100 条演示后训练 100 轮。')],
 example='教学推演：回到上面的场景。第一步，能做这件事的前提是有数据：作者先把平台造出来——双臂轮式机器人、每只手 10 个驱动自由度、掌面与指内侧 268 个压阻触点，遥操作用骨架手套加改过的 DexPilot 重定向——才采到 200 小时、10576 条轨迹。第二步，触觉被画成一张图：268 个触点按它们在手上的位置摆进 224×224 的画面，没有触点的地方填零。这张图和腕部 RGB 一起做掩码重建，各遮 75%；若只在触觉上做，模型会发现一律输出零就能得分，加上视觉这条捷径才不成立。第三步，翻转耳机时真正有读数的只有拇指与食指的那几个触点。接触门把没超过阈值的空间 token 全部屏蔽，只让被触发的进前缀，另外保留每只手一个全局 token；这个全局 token 不许去注意视觉与语言，只允许别人注意它，为的是让它始终代表触觉本身。第四步，训练时还要求模型从这些 token 预测第 10、20、30、40、50 步之后的法向力，摊在 1.67 秒上而不是挤在最近 0.17 秒里，后者太容易靠外推蒙对。第五步，推理时十步 Euler 去噪出一块动作，落成末端位姿与手指关节位置。做不到的事同样清楚：耳机翻到一半掉了，策略内部没有任何模块会发现；而小指与无名指的指缘根本没贴触点，多物体抓取的成功率因此只有 40%，作者把这一条写进了局限。',
 code='backbone = π0.5 检查点 (PaliGemma Gemma-2B + 300M 动作专家)\ntac_enc  = ViT，在自采 200 小时数据上与 RGB 做掩码联合重建（各遮 75%）\n# 动作投影层从 32 维丢弃重初始化为 64 维，其中 34 维实际使用\n\nfor hand in (左, 右):\n    S = tac_enc(tactile_image(hand))   # 196 个空间 token + 1 个全局 token\n    S = [t for t in S if 任一触点 > 接触阈值] + [global_token(hand)]\nprefix = 三路图像(768) + 语言与离散状态(≤350) + 触觉(394)\n# 全局 token 不注意视觉/语言/动作，它们可以注意它\n\nA = flow_match(expert, prefix, steps=10)  # 长 50：末端位姿 + 手指关节，相对当前状态\nexecute(A)                                # 执行后没有成功检测，也没有重试逻辑\n# 训练时另有辅助头：从触觉 token 预测第 {10,20,30,40,50} 步的法向力',
 setup='四项真机灵巧任务是耳机翻转、叠书取书、明信片取放与多物体抓取，每种方法各 20 次试验，初始摆放随机但各方法共用同一批。评测用的物体从 200 小时预训练数据里留出，每项任务另用 100 条演示后训练。下表取自表 I 与表 III。',
 headers=['设置 / 方法', '平均成功率', '平均完成率'],
 rows=[['π₀.₅ 官方权重直接后训练（表 I，四项）', '28%', '44%'], ['π₀.₅ 加 200 小时数据，不用 STAR（表 I，四项）', '44%', '61%'], ['STAR 完整配方（表 I，四项）', '61%', '79%'], ['去掉触觉输入（表 III，两项均值）', '45%', '61%'], ['推理时屏蔽触觉（表 III，两项均值）', '53%', '69%']],
 findings='61% 的均值盖住了任务间的落差：明信片取放 85%，耳机翻转 65%，多物体抓取只有 40%，而后者完成率却有 72%，作者归因于小指与无名指的指缘没贴触点。表 III 还显示去掉触觉后叠书取书的成功率不变，触觉并非在每项任务上都带来收益。',
 ablation='表 II 在两项任务上逐个拆开：去掉全局 token 平均成功率从 60% 掉到 30%，去掉稀疏筛选掉到 33%，去掉视触觉联合预训练掉到 38%，三者一起去掉是 35%。未来触觉预测影响最小，去掉仍有 53%，换成密集的近期五帧反而降到 48%。',
 boundary='模型来源分得很清楚。现成的是 π₀.₅ 检查点，PaliGemma 的 Gemma-2B 主干加 300M 动作专家，流匹配目标与去噪步数原样保留；掩码自编码架构、M2VTP 的联合预训练做法、DexPilot 重定向与 T-Dex 的触觉成像也都是借来的方法。自训的是触觉编码器本身，在自采数据上用 batch 1024 训 15000 步约一轮，此外还有可学习的全局 token、投影层、未来力预测头，以及整个策略的两段训练——200 小时上 6 轮加每任务 100 轮。真正的前提成本是数据与硬件：200 小时、10576 条轨迹、65 个任务全部自采，依赖骨架手套、改过的重定向算法与一整套双臂灵巧手平台，而论文只给了 batch size 与轮数，卡型与卡数没有给出。基线口径也要留意：GR00T N1.7 记零是因为收敛不好、出现不安全振荡而被中止试验，这个零不宜当作它的能力上限。',
 transfer='与 N₀-VTLA 并排读：一个把触觉压成预测目标不进前缀，一个把触觉筛进前缀再预测未来力；对照 π₀.₅ 则可见两篇换的都是条件而不是编排。',
 exercise='先统计自己数据里触点的激活比例，再按接触门屏蔽未激活 token，比较前缀长度与成功率的变化。',
 answer='若屏蔽后成功率不降反升，说明冗余 token 确实在稀释注意力；若明显下降，要先查阈值是不是把弱接触一并滤掉了。',
 sources=[('数据与平台：§IV-A', 'S4.SS1'), ('训练配方：§IV-B', 'S4.SS2'), ('基线对比：表 I', 'S5.T1'), ('消融：表 II–III', 'S5.T2')])

add('ultra', version='v2',
 takeaway='一套参数同时吃稠密参考与稀疏目标，缺哪一路观测就把哪一路遮掉。',
 result='真机 Unitree G1：稠密参考跟踪 73%，稀疏目标跟随用动捕物体位姿是 80% 与 90%，换成第一人称点云降到 50% 与 60%。',
 why='前两篇讨论的是怎样编排执行器，这篇讨论的是执行器本身能做到什么。它不引入任何高层模型，29 维关节目标全部由一条策略产生，没有自由度交给传统全身控制器，因此它给出的是分层系统里最底下那一层的上限与失败模式。',
 scene='让人形把一只箱子从这里搬到那里。手里没有逐帧的参考动作，只有一个物体的目标位姿；物体的位置也不是动捕给的，要从头部深度图里抠出来。只会跟踪参考轨迹的控制器在这里无从下手，因为参考根本不存在。',
 recall_module='改的是控制器的条件接口，而不是在它上面再加一层。稠密参考、长程物体目标、长程根部目标、下一帧局部状态变化与离散命令被统一切成 token，再配一份可用性掩码决定哪些进注意力；缺哪一路就遮掉哪一路，同一套参数因此能在跟踪、目标跟随与视觉操作三种模式间切换。潜变量走变分瓶颈，负责在稀疏目标下补齐被留空的中间动作；局部跟踪另走一条残差捷径绕开它。动作出口没有变：29 维关节目标位置交给 PD 控制器，没有任何自由度留给传统全身控制器。',
 recall_io='输入分五块：本体感觉与十步历史、全局根部残差、离散命令、局部关节残差，以及物体侧的位姿残差与 64 点点云，末尾跟着一份掩码，总维度 1496。输出是 29 维关节目标位置，以约 59 Hz 交给 PD 控制器，中间没有轨迹规划，也没有另一层全身控制器。训练时潜变量由特权后验与先验相加，部署时只从先验采样。失败通路要分两处说。训练里有一条：早停条件是摔倒、偏差过大或接触不匹配持续 20 帧，微调阶段的目标项按掩码门控，看不见的目标不计奖励也不进成功判定。运行时则没有失败检测与恢复，没有任何模块回答刚才那一下做成没有，策略只能在下一帧观测里继续闭环，靠域随机化与推搡训出来的裕度吃掉扰动。作者列出的真机失败是摩擦不足导致抓取滑脱、深度噪声与遮挡让点云抽取失效，以及超出恢复裕度的扰动；动捕模式下标记点被遮还会让物体位姿抖动漂移，级联成不稳定的跟踪。',
 steps=[('重定向：把 RL 当成带物理约束的轨迹优化', '一条统一的 RL 策略把人与物体的动捕序列变成目标本体上物理可行的 rollout，不做逐条轨迹的优化，也不为新数据重训。奖励是末端锚点位置、链接方向、物体位姿、掌面到物体表面的偏移、接触事件对齐与能量正则六项相乘，运动学、动力学与接触由仿真器的转移本身强制，不写成显式约束。'), ('特权教师：换成全链接跟踪，并被反复打断', '另训一条特权教师去跟踪这些 rollout。它用部署时的控制接口与力矩上限，训练时却看得到完整状态与稠密参考残差；奖励模板不变，稀疏锚点换成全链接跟踪。为了让学生学到带恢复的行为，这一阶段随机化物理属性并注入推搡，早停条件是摔倒、偏差过大或接触不匹配持续 20 帧。'), ('学生：多模态切成 token，可用性掩码决定哪路生效', '学生只看部分观测：本体感觉、目标、物体状态与点云各自编码成 token，由轻量 transformer 融合，掩码决定哪些参与注意力。目标那一路含长程物体变换、长程根部变换与下一帧局部状态变化。蒸馏走 DAgger，采样由教师渐变为学生；潜变量由特权后验与先验相加，部署只取先验。'), ('RL 微调：一部分环境改跑目标到达', '在蒸馏好的学生之上做 PPO 微调，把一部分并行环境切成目标到达，其余继续做蒸馏更新以保住先验。物体目标、根部目标与各自的初始状态随机取偏移，点云上叠抖动、丢点与遮挡。奖励是稠密接近项加逐步推进项；关键是目标项按掩码门控，看不见的目标既不计奖励，也不进成功判定。'), ('部署：同一套参数靠掩码切三种模式', '测试时只喂学生自己的观测，潜变量从先验采样。解开局部参考就是高保真跟踪，遮掉局部参考、放开长程目标就是目标条件控制，遮掉动捕物体位姿、放开点云就是视觉操作。点云从深度单独抽出来：反投影、裁前向区域、去地面、取最大簇当作箱子。动作是 29 维关节目标位置，交给 PD 控制器。')],
 example='教学推演：回到上面的场景。第一步，这次搬运的示范不是人直接演给机器人看的：一条 RL 重定向策略把人与物体的动捕序列在仿真里跑成目标本体上物理可行的 rollout，奖励把末端锚点、物体位姿、掌面到物体表面的偏移与接触事件对齐乘在一起，物理可行性由仿真器保证，而不是写成运动学约束。第二步，另训一条特权教师去跟踪这些 rollout，它用部署时的控制接口与力矩上限，但看得到完整状态与稠密残差，并被随机化与推搡反复打断，好让后面学到的是带恢复的行为。第三步，学生只看得到部分观测，本体感觉、目标、物体位姿与 64 点点云各自编码成 token，掩码决定哪些进注意力；采样从教师逐步过渡到学生，潜变量由特权后验与先验对齐训练，部署时只取先验。第四步，微调把一部分并行环境切成目标到达，目标与初始状态随机偏移，点云上叠抖动、丢点与遮挡，目标项按掩码门控，看不见就不计分。第五步，回到这次搬运：把稠密参考那一路遮掉，只留长程物体目标与点云，策略每步出 29 维关节目标位置交给 PD 控制器，箱子的位置由深度图去地面、取最大簇得到。要说清没做到的：整条链上没有任何模块回答刚才那一下做成没有，扰动只能靠训练时留下的裕度吃掉；箱状物体与刚性手是硬前提；真机试次少到个位数，从动捕换成第一人称点云就掉三十个百分点，这一段差距是感知的，不是控制的。',
 code='# 阶段一 重定向：单一 RL 策略把人—物动捕变成本体上物理可行的 rollout\nr_track = r_p · r_r · r_obj · r_int · r_ct · r_eng   # 末端锚点 + 物体 + 接触对齐\n# 阶段二 特权教师：同一奖励模板，稀疏锚点换成全链接跟踪；加随机化与推搡\n# 阶段三 蒸馏：DAgger 采样，学生只看部分观测\no_s = [proprio, goal, object, pcd, m]    # m 是可用性掩码，缺哪一路就 gate 掉\nz   = z_prior(m(o_s)) + z_res(o_s, o_teacher)   # 64 维潜变量，部署只取 prior\nL   = |a_s − a_teacher|² + λ_KL·KL(q ‖ p) + 辅助重建   # λ_KL 由 0.001 升到 0.1\n# 阶段四 RL 微调：部分环境改跑目标到达，目标项按掩码门控，看不见就不计分\nfor t in 部署:\n  o_s = 遮掉稠密参考，只留长程物体目标与点云\n  a_t = pi_student(o_s, z ~ prior)     # 29 维关节目标位置\n  执行(PD(a_t))                        # 没有任何自由度交给传统全身控制器\n# 成功判定：不摔倒，且末态与目标的距离小于 0.3 m',
 setup='仿真在 IsaacGym 训练、MuJoCo 复核，真机是 Unitree G1。数据取一份人与物体动捕的修正子集，只用四类箱状物体，经轨迹与物体缩放增广到约 6 倍。表 IV 是真机结果，稠密跟踪不分方向合成一个数字，稀疏目标跟随分纵向与横向两列。',
 headers=['表 IV · 真机设置', '纵向', '横向'],
 rows=[['稠密参考跟踪（不分方向）', '73%（19/26）', '同一条目'], ['稀疏目标跟随 · 动捕物体位姿', '80%（8/10）', '90%（9/10）'], ['稀疏目标跟随 · 第一人称点云', '50%（5/10）', '60%（6/10）']],
 findings='同一套参数下，把物体位姿从动捕换成头部深度点云，稀疏目标跟随从 80% 与 90% 掉到 50% 与 60%，这一档差距衡量的是感知而不是控制。表 IV 的表注写每项两次试验，分母却是 26 与 10，论文没有解释两者怎么对上，这些小样本数字不宜再做横向比较。',
 ablation='表 III 在 MuJoCo 上每设置取 20 条动作，只拆 RL 微调一项：同分布目标下点云 16/20 升到 19/20、纯位置 14/20 升到 16/20；换成带随机偏移的分布外目标，是 5/20 升到 9/20 与 4/20 升到 12/20。收益集中在分布外。',
 boundary='模型来源这一篇最干净，也最该看清成本落在哪里：没有接入任何现成的语言或视觉语言模型，重定向策略、特权教师、多模态学生与 RL 微调四个阶段全部由作者训练，点云走一个从头训的 PointNet，另一个小型语言编码器只用在潜空间的语义聚类分析上，不参与控制；作者在限制里自己写明，未来应当接入预训练感知模型而不是从头训。前提成本因此集中在仿真与数据：数千个并行环境、约 59 Hz 的控制频率、整套域随机化与点云噪声表，数据是一份人与物体动捕的修正子集，只覆盖四类箱状物体，手是刚性的，增广后约为原来的 6 倍。可比性要单独看：仿真与真机不是同一套任务，表 III 是每设置 20 条动作的 sim-to-sim，表 IV 是真机，表注写每项两次试验却给出 19/26、8/10 这样的分母，论文没有解释。成功判据是不摔倒且末态距目标 0.3 米以内，没有中间进度指标，差一点点与完全没动在数字上一样。',
 transfer='放在 π₀.₅ 与 RT-2 那一层读：先看清被调用的执行器接受什么指令，再回头看 Cybo-Waiter 与 Spatial Brain 塞给它的是什么。',
 exercise='固定一条全身策略，把一个长程目标换成一串中途路点，比较整段成功率与摔倒次数的变化。',
 answer='把目标切碎通常抬高单段成功率，却让接缝变多。摔倒次数与整段成功率要分开记，只看其中一个常会得出相反的结论。',
 sources=[('方法四阶段：§IV', 'S4'), ('多模态学生策略：§IV-C', 'S4.SS3'), ('目标跟随与微调消融：表 III', 'S5.T3'), ('真机结果：表 IV', 'S5.T4')])

add('cybo-waiter', version='v1',
 takeaway='把子任务的完成条件写成几何谓词，连续若干帧成立才判完成。',
 result='Being-0 对齐五项任务各 10 次，抓瓶 8/10 升到 10/10、放篮 6/10 升到 9/10；长程三项去掉监督者各降两次。',
 why='如果你已经写过带验收环节的 Agent，这篇值得看的是验收条件从哪里来。它不让模型事后看一眼图说做成了，而是在规划的那一刻就把每个子任务的前置与成功条件写成带稳定帧数的谓词，判定交给分割与深度算出的几何量。',
 scene='人形站在杂乱的办公桌前收拾东西，指令是把杯子放进收纳盒。松手那一瞬深度图恰好抖了一帧，杯子的掩码丢了。若按动作原语返回正常就算做完推进，机器人会空着手走向下一个物体，杯子还躺在桌上。',
 recall_module='改动落在验收这一层，而不是机器人会做什么。计划不再只是一串动作名，每个子任务自带前置条件与成功条件，写成参数化谓词并各带一个必须连续成立的帧数；判定依据是 SAM3 分割后再由 RGB-D 抬到三维的质心、尺寸、置信度与成对关系。它没有改动作本身：下肢那条步态条件 RL 策略与上半身 MPC 都在原地，运动侧固定成 7 条原语。VLM 语义校验只在几何证据缺失或矛盾时被叫起来，且不作为主判据。',
 recall_io='输入是一条自然语言指令与头部 RGB-D 观测，加上机器人本体状态；规划器看到的是指令与观测，监督者看到的是当前子任务、目标句柄与那份以物体为中心的工作空间状态。输出分两级：规划器输出 typed JSON 的子任务序列，执行层按类型把它派成下肢的 gait_id 与底盘速度、偏航率指令，或上半身 MPC 的末端轨迹，几何目标由句柄提供，走位与操作之间由一个轻量协调器按类型与监督者反馈切换。终止条件是成功条件的全部谓词连续 stable_frames 帧成立。失败通路是这篇的主线：监督者每帧算一遍谓词的标量度量，前置不成立记 blocked 并报出是哪几条，超时未完成或安全谓词被违反记 failed，几何估计不可靠时抬起不确定标志；随后按失败类型选恢复算子，重新观测、重新落点、换原语或收紧容差、插入纠正子任务；重试预算由 max_retry 定，用尽才把这条反馈记录交回规划器重规划，已完成的子任务保留。需要留住的是判定链条全在几何上，掩码丢了这条链就断了。',
 steps=[('指令先编译成带谓词的 JSON 计划', 'VLM 规划器把指令编译成一串定长字段的子任务：类型、目标的符号句柄与语言描述、目标区域、manipulation 与 locomotion 两个块、前置与成功条件、超时秒数与最大重试次数。条件写成谓词断言，各带一个 stable_frames；论文没有给出这个 VLM 的型号。'), ('落地的不只是目标物，谓词提到的都要落', '当前子任务涉及的实体不止目标：目标区域、target.relations 提到的物体，以及前置与成功条件参数里出现的物体，例如 SUPPORTED_BY 的支撑面，都要落。每个实体按语言描述去 SAM3 取候选掩码，掩码内的深度像素反投影成点云，算出质心、尺寸与一个合成置信度。'), ('监督者按稳定帧判定，并给出条件级诊断', '每条谓词先折成一个标量几何度量，与目标值比出瞬时真假，再要求连续 n 帧都成立才算满足。前置全满足记就绪，成功条件全满足记完成，前置不成立记 blocked，超时或安全谓词被违反记 failed。另外，监督者还回一组连续诊断：高度差、相对距离、对齐角与置信度，指出哪一条不满足。'), ('恢复算子先上，预算用尽才整段重规划', '按失败类型选恢复算子：重新观测、用更新后的提示重新落点、换一条原语或放宽容差、对齐误差很小时微调末端位姿，再不行就往剩余计划里插纠正子任务。重试预算由子任务自带的 timeout_sec 与 max_retry 决定，用尽才把失败谓词与诊断交回规划器重规划，已完成的子任务保留。'), ('执行层：下肢是步态技能库，上肢走 MPC', '执行层按子任务类型派活。LOCOMOTION 走步态条件的 RL 策略，按 gait_id 输出下肢指令跟踪底盘速度与偏航率，12 段片段映射成 7 条原语。MANIPULATION 按计划指定的手与原语走上半身 MPC 生成末端轨迹，下半身独立维持平衡，两者由轻量协调器切换。')],
 example='教学推演：回到上面的场景。第一步，指令先被编译成 typed JSON：一个 MANIPULATION 子任务，target 的 phrase 写 cup、destination 写 container，manipulation 块指定右手、PLACE 原语、启用上半身 MPC，前置条件是 VISIBLE 连续 3 帧成立，成功条件是 SUPPORTED_BY 连续 10 帧成立，超时 30 秒、最多重试 2 次，论文附的计划片段里就是这么写的。第二步，落地的不只是杯子：谓词参数里出现的支撑面、目标区域与关系引用一起送进 SAM3 取候选掩码，再用深度反投影成点云，算出质心、尺寸与置信度，按语义、关系、几何、时间稳定四项加权选实例。第三步，监督者每帧把 SUPPORTED_BY 折成一个标量再二值化，掩码丢的那一帧只让它变成 0，连乘之后成功条件不满足，状态仍是执行中，朴素做法在这里就提前推进了，稳定帧要求挡住的正是这一下。第四步，如果一直超时，诊断向量会指出是高度差还是对齐角不对：小误差走末端微调，估计不可靠走重新观测与重新落点，都不行才在重试预算用尽后交回规划器重规划。第五步，执行侧始终是分开的：走位时调 7 条运动原语之一，操作时上半身解 MPC、下半身独立维持支撑，协调器按子任务类型与监督者反馈切换。要说清没做到的：这套判据全部建在几何上，有没有握牢、有没有滑脱只能靠掩码与深度间接推断；站不站得住不在任何一条谓词里；论文也没有给出 VLM 的型号、恢复算子被触发的频次，以及摔倒这类失败的单独统计。',
 code='plan = VLM(instruction, obs)        # typed JSON：每个子任务带谓词与 stable_frames\nfor tau in plan.subtasks:\n  for attempt in range(tau.max_retry + 1):\n    W = ground(SAM3(tau.entities), depth)   # 掩码反投影成质心、尺寸、置信度\n    if not all(sat(p, W) for p in tau.preconditions):\n      coordinator.locomote(tau.locomotion); continue   # blocked：先走位再来\n    execute(tau.manipulation)       # 上半身 MPC，下半身独立保平衡\n    while t < tau.timeout_sec:      # 终止条件：成功条件连续 n 帧成立\n      W = ground(SAM3(tau.entities), depth)\n      if all(sat(g, W) for g in tau.success_conditions): done = True; break\n    if done: break\n    apply(tau.failure_handlers)     # 重新观测 / 重新落点 / 换原语 / 微调位姿\n  if not done:\n    plan = VLM.replan(tau, status, failing_predicates, diagnostics)',
 setup='真机平台是 Unitree G1 配 Dex3-1 灵巧手与头部 RGB-D，感知与决策跑在一台 RTX 5080 工作站，低层控制器跑在机身计算机，场景是杂乱的室内办公环境。表 I 按 Being-0 的任务定义与判据执行，每项 10 次；表 II 是三项自定义长程任务，同样每项 10 次。',
 headers=['表 I · 任务（各 10 次成功计数）', 'Being-0', 'Cybo-Waiter'],
 rows=[['Fetch-bottle', '9/10', '9/10'], ['Grasp-bottle', '8/10', '10/10'], ['Place-basket', '6/10', '9/10'], ['Place-coffee', '6/10', '8/10']],
 findings='两张表都是 10 次的成功计数，样本小到一次成败就改 10 个百分点。表 I 里取瓶与送篮两项与 Being-0 持平，收益集中在放篮、放咖啡这类要判断物体是否稳稳落到支撑面上的子任务，与谓词监督的作用方向一致。',
 ablation='表 II 只拆了监督者一项：关掉之后 Tidy-desk 从 7/10 降到 5/10、Tabletop-sorting 从 8/10 降到 6/10、Bring-me-a-drink 从 9/10 降到 7/10。谓词、稳定帧、多物体落点与恢复算子没有分开消融，因此不知道收益来自哪一件。',
 boundary='模型来源要分三块看。感知侧是现成的 SAM3，几何量由标定过的 RGB-D 反投影得到，不训练；规划与语义校验用的 VLM 同样是现成模型，但论文自始至终没有给出型号。真正由作者训练的只有下肢那条步态条件 RL 策略：12 段动作片段在仿真里配域随机化训出来，对外暴露成 7 条运动原语；上半身走 MPC，不是学出来的。因此前提成本主要在工程：谓词库、稳定帧阈值、恢复算子、两台机器的部署，以及每个子任务的 timeout_sec 与 max_retry 怎么填，这些值论文没有给出。评测也要看清楚：两张表都是每项 10 次的成功计数，表 I 按 Being-0 的定义与判据执行，但论文没有说对照那一列是自己复现还是引用。失去平衡与摔倒这类人形特有的失败没有单独统计，成功率之外也没有任务进度指标，因此看不出失败发生在哪一步。',
 transfer='与 RoboHarness 对照读：那边的交接目标取在支撑域与可达集的交集里，这边的前置谓词只管几何成不成立，站不站得住不在判据内。',
 exercise='写一个两物体的假场景，给成功谓词设 1 帧与 10 帧两种稳定长度，注入几帧检测抖动，分别统计误判与多等的时间。',
 answer='稳定帧数换的是误接受率与延迟，不存在免费的一档。同时记录提前推进的次数与每个子任务的平均耗时，才看得出这个阈值该怎么定。',
 sources=[('方法总览：§III', 'S3'), ('执行层与步态技能库：§III-E', 'S3.SS5'), ('Being-0 对齐任务：表 I', 'S4.T1'), ('长程任务与监督者消融：表 II', 'S4.T2')])

add('spatial-brain', version='v2',
 takeaway='把视角也当成可调的动作，先主动看清楚，再决定下一步派谁去做。',
 result='五类任务的难档，本文 60.0/70.0/55.0/60.0/60.0，数据驱动基线 Ψ₀ 为 35.0/45.0/25.0/0/30.0。',
 why='这篇把看不清当成一个可以主动解决的问题：相机颈部 2 个自由度，底盘另有 4 个，视角由 VLM 直接决定。对 Agent 研究者，它提供的是另一种感知接口，不是把一帧观测交给更强的模型，而是让规划器自己决定再看哪里。',
 scene='人形要去够柜子深处的一只杯子，杯子被前排的盒子挡住，从当前站位的视野里根本不在画面上。只拿这一帧去问模型杯子在哪，它要么答不出，要么凭常识猜一个位置，机器人照着走过去伸手，够到的是空气。',
 recall_module='改的是观测从哪里来，以及全身动作由哪几个现成能力拼出来。上层不再被动接受一帧图像：视角被写成一个 6 自由度的动作空间交给 VLM 决定，记忆库按底盘是否移动决定哪些图像还算数。下层把全身拆成相机、灵巧手、上肢、下肢四路，每一路各由一个不需要任务数据的 agent 产生，下肢最终只暴露前进速度、偏航率与身高三个量。没有被改的是这些部件本身：Hyper3D、EasyHOI、A*、IK 与那条沿用 HOMIE 的 RL 策略都按原样接入。',
 recall_io='输入是语言指令、RGB-D 观测与本体状态。大脑看到的是当前图像、记忆库里这次站位以来的图像与相机位姿，以及一份记录全部下发指令与反馈的文本日志。输出是相机动作、灵巧手姿态、上肢关节与下肢关节四路，最后一并交给 PD 控制器出力矩。高低层之间传的不是关节轨迹：大脑发的是子任务命令加对某个 agent 的调用，导航与可达空间求解器把它化成前进速度、偏航率与身高三个量交给 RL 策略，上肢那边传的是抬到三维的语义关键点与选中的原语，由运动学规则展开成 6 自由度末端位姿再走 IK。交接处的保障只有一条，就是求解器那个硬约束：肩到物体的距离不超过臂长的 0.8 倍；动力学上站不站得住不在这个约束里。失败靠规划器每步重新评估：抓空之后，它从记忆库读出这一步的反馈与当前画面，判断阶段没有推进，于是再调一次操作 agent 重试。重试上限与失败判定的具体依据，论文没有给出。',
 steps=[('主动感知：视角本身是一个动作', '论文把机器人的观察动作连同各自的效果一起写进提示交给 VLM：颈部 2 个自由度，加上底盘沿三轴的平移与绕竖直轴的旋转共 4 个自由度。VLM 直接从当前图像与指令决定往哪看，于是遮挡与视野外的目标靠改变视角解决，而不是靠更强的单帧识别。'), ('记忆库只留这次站位以来的图像', '底盘一动，视角就整体改变，旧图像会误导 VLM。记忆库因此只保留自上次底盘移动以来拍到的图像，每张配上对应的相机位姿；另有一份文本日志，记下大脑发给小脑的全部指令与各自的反馈。规划器靠这两样判断任务进行到哪一步。'), ('自适应规划：每步只在换视角与派活之间选一个', '规划器先把长程目标拆成子任务序列，再进入闭环：每一步拿记忆库比对这个序列判断当前阶段，然后要么调整视角看全，要么调用小脑里的一个动作 agent 推进。初始计划可能因观测不足而不可行，执行也可能被扰动打断，所以每一步都重新规划；论文举的例子是抓空后再调一次操作 agent 重试。'), ('下肢：远近两套求解，最后只出三个量', '远处交给导航 agent：深度图抬成点云投到地面网格，超阈值的格记占据，A* 求无碰路径，RDP 简化成路点。近处交给可达空间求解器，解一个把物体拉进肩部工作空间的优化，硬约束是肩到物体不超臂长的 0.8 倍。两者化成前进速度、偏航率与身高，交给沿用 HOMIE 的 RL 策略。'), ('上肢：抓取先过人手图像，抓后走四条原语', '抓取 agent 先让生成模型合成一张手物交互图，再用 Hyper3D 重建物体网格、EasyHOI 估出人手姿态，按指尖对齐重定向成灵巧手。抓后轨迹另有一个 agent 预测二维关键点并选原语：推、拉、放、转；关键点抬到三维后按运动学规则展成末端位姿，再由 IK 转成关节指令。')],
 example='教学推演：回到上面的场景。第一步，大脑看到杯子不在画面里，于是动用那 6 个自由度：先转颈部扫一遍，不够就让底盘平移或升降换一个站位，视角决策直接由 VLM 从当前图像与指令给出。第二步，底盘一动，记忆库把此前的图像清掉，只留这次站位之后拍到的几张与对应相机位姿，另一条文本日志继续记着已经下发过哪些指令、各自回了什么，规划器靠它判断现在进行到第几步。第三步，看清之后规划器不再换视角，改为派活：先导航到柜前，再精调站位，然后抓取。第四步，下肢这一段分两截，远处用 A* 在地面占据网格上求路径、RDP 简化成路点，近处用那个优化求出底盘该往哪挪、身高该升降多少，硬约束是肩到杯子的距离不超过臂长的 0.8 倍；两截最后都化成前进速度、偏航率与身高三个量，交给沿用 HOMIE 的 RL 策略变成下肢关节动作。第五步，上肢先合成一张人手抓这只杯子的图像，经 Hyper3D 与 EasyHOI 拿到物体网格与人手姿态，再按指尖对齐重定向成灵巧手构型；抓到之后选放这条原语，关键点抬到三维、按规则展开成末端位姿序列，走 IK 下发。要说清没做到的：交接处只保证几何可达，那条 0.8 倍臂长的约束不管动力学，站得到不等于站得住；抓空之后靠规划器重调一次操作 agent，但重试上限与失败判定的依据论文没有给出；成功判据是完成操作且全程不碰障碍物，失去平衡没有单列，也没有任务进度指标。',
 code='plan = Brain.decompose(instruction, obs)   # VLM，论文没有给出部署用的型号\nwhile plan 未完成:\n  stage = Brain.locate(memory)   # 记忆：本次站位以来的图像 + 全部指令与反馈\n  if 观测不足:\n    a_cam = Brain.move_view()    # 颈部 2 自由度 + 底盘 4 自由度，主动换视角\n    memory.add(image, camera_pose); continue\n  g = plan.next()\n  if 目标不在可达域:\n    waypoints = RDP(Astar(grid_map(depth)))        # 导航 agent，远距离粗接近\n    dd, dz = solve(min λ1|Δd|² + λ2Δz² + λ3|p_obj − p_shoulder|²,  ≤ 0.8L)\n    pi_loco(s, c=[v_x, ω_yaw, h])  # 下肢接口只有三个量，沿用 HOMIE 的 RL 策略\n  G = retarget(EasyHOI(gen_image(obs, g)), Hyper3D(obs))   # 人手抓取图→灵巧手\n  poses = primitive(keypoints_3d)  # push / pull / place / rotate 四选一\n  execute(IK(poses)); memory.log(g, feedback)   # 反馈回大脑，抓空就再调一次',
 setup='真机平台是 Unitree G1，配两只 6 自由度 Inspire FTP 手与一台 2 自由度主动 RealSense D435i。五类任务设置各分易、难两档，每设置 10 至 30 次；判为成功要求完成操作且全程不与障碍物碰撞。表 1 的任务 3 是高度加位置，任务 4 是避障。',
 headers=['表 1 · 方法', '任务 3 易', '任务 3 难', '任务 4 易', '任务 4 难'],
 rows=[['TrajBooster', '55.0', '10.0', '60.0', '0'], ['Ψ₀', '70.0', '25.0', '70.0', '0'], ['CaP', '70.0', '35.0', '80.0', '30.0'], ['本文', '80.0', '55.0', '80.0', '60.0']],
 findings='易档四种方法挤在 55 到 85 之间，分化全在难档：Ψ₀ 在避障难档是 0，本文是 60.0。这更像数据驱动基线离开微调分布后的塌陷，而不是本文在同一难度上领先多少；两档各自的任务数与试验次数论文没有逐项给出。',
 ablation='表 3 只拆主动感知与可达空间求解器两项：两个都关是 18.8 与 0，只留主动感知是 55.6 与 16.7，只留求解器是 24.9 与 33.4，都开是 67.5 与 70.0。两项各自主导一类任务，但这两列与表 1 对应任务的易、难两档对不上，论文没有说明怎么合并。',
 boundary='模型来源这一层几乎全是现成的，但有两处论文没有写清。大脑写的是主流 VLM，论文没有给出部署时实际用的是哪一个；表 4 评了 gpt5.1、gemini-3-flash、gemini-3.1-pro 与 qwen3.5-plus 四个，却没说框架跑的是其中哪一个。小脑侧：物体网格用 Hyper3D，人手姿态用 EasyHOI，手与物体的交互图由多模态生成模型合成，导航是 A* 加 RDP，肩部偏移用 MuJoCo 标定，下肢那条 RL 策略沿用 HOMIE，论文只说沿用，没有说是直接取公开权重还是自己重训。所以不需要任务相关真机数据成立于这套流水线，不等于整条链上没有训练过的部件。评测侧也要分开看：每设置 10 至 30 次，论文没有给出每项具体多少次；判据是完成操作且全程不碰障碍物，摔倒与失去平衡没有单列，也没有任务进度指标；表 3 的两列与表 1 对应任务的两档对不上，论文没有说明合并方式。',
 transfer='与 RoboOS 对照：那边靠共享记忆让多台本体看见同一个世界，这边的记忆一移动底盘就清空图像，只留文本，跨站位的空间一致性得重新看一遍。',
 exercise='在一个有遮挡的桌面上，比较固定视角与允许两次主动改视角的定位成功率，并记录多花的时间与推理次数。',
 answer='主动感知的收益应当与它的时间成本一起报告。表 4 里平均观察次数从 2 次到 4.8 次不等，每次都要重新拍与重新推理，次数就是这一层的预算。',
 sources=[('框架与两层分工：§3.1', 'S3.SS1'), ('动作小脑：§3.3', 'S3.SS3'), ('主结果：表 1', 'S4.T1'), ('组件消融：表 3', 'S4.T3')])

add('simple', version='v1',
 takeaway='给人形全身任务做仿真评测，先要交代动作接口切在哪里、成功由哪条几何判据说了算。',
 result='六项任务三档随机化、每格十次试验，Ψ0 与 ACT 靠前，GR00T N1.6 在移动取放上六格全零；真机只补测两项。',
 why='这篇不提出更强的人形策略，它提出的是一套让不同策略能被摆在一起比的条件。对 Agent 研究者，它回答的是被调用的那条全身策略究竟在什么接口上被测过：哪几维由它输出，哪几维由控制器接管，成功按哪条阈值判。',
 scene='两篇论文都说自己的人形策略能把杯子从高架搬到低架。一篇在真机上跑了十次，一篇在自建仿真里跑了一百次；一篇的策略自己出腿部关节，一篇只出一个底盘速度指令，腿由现成控制器走。把两个数字并排放进一张表，比的其实不是策略。',
 recall_module='改的是评测这一层，不是又提出一条更强的人形策略。它把全身操作的评测条件固定下来：物理交给 MuJoCo、渲染交给 Isaac Sim，动作空间统一成 32 维上半身目标加 4 维运动指令，下肢关节一律交给 AMO 或 SONIC 这两条现成控制器，成功由任务自带的几何判据程序化判定，随机化分三档。它不训练控制器，也不改被评策略的架构；唯一由作者训练的，是为了填表而对九条公开检查点做的单任务微调。',
 recall_io='输入是环境每步给出的观测：Isaac Sim 渲染的 RGB 图像、32 维本体状态（双手 14、双臂 14、腰部三轴、底盘高度）与任务的自然语言指令，策略以远程服务进程的形式接收它们。输出是 36 维动作块，前 32 维是上半身关节目标，后 4 维是 vx、vy、vyaw、qyaw；它们不直接落到腿上，而是先进 AMO 或 SONIC，由下肢控制器换算成 500 Hz 的关节位置命令，同时负责平衡与迈步。失败这条通路要写清楚：环境里没有成功检测器反馈给策略，也没有重试或复位机制，check_success 只在外部统计成绩。一次抓空之后，策略拿到的仅仅是下一帧观测，纠正只能来自它自己在新观测上重新预测动作；如果机器人失去平衡，论文没有给出终止规则，也没有说这类试验如何计分。所以表 1 的数字是十次独立试验的二值计数，既没有分阶段进度，也读不出失败发生在哪一步。',
 steps=[('物理与渲染分给两个仿真器', 'MuJoCo 按 500 Hz 跑刚体动力学与接触求解，每步把机器人和物体状态同步给 Isaac Sim，后者按 50 Hz 做光线追踪渲染出 RGB 观测。整个循环包在 Gymnasium 接口里，每步之后由 Task 给出奖励与终止信号。两个仿真器与场景物体资产都是现成件。'), ('资产与任务在离线阶段一次备好', 'Objaverse 与 GraspNet-1B 的网格先做 CoACD 凸分解生成碰撞几何，再丢进仿真确定稳定放置姿态，另一路转成带贴图的 USD 给 Isaac Sim；BoDex 据此离线合成抓取位姿并缓存。六十项任务铺在 50 个 HSSD 场景上，评测时不做在线合成。'), ('两条采数据的通路，腿都不归操作者直接管', '自动通路由 CuRobo 出双臂轨迹，脚本化下肢策略配合底盘移动；VR 通路改用 MuJoCo 把第一人称立体视频推给 PICO 头显，操作者手部经逆运动学映到上半身，平衡与行走由 AMO 或 SONIC 按摇杆自行处理。演示按 50 Hz 录成 LeRobot 格式。'), ('控制权按三十六维动作向量切开', '策略输出 36 维：32 维上半身目标，含双手 14、双臂 14、腰部三轴与底盘高度，加 4 维运动指令 vx、vy、vyaw、qyaw；腿部关节不在其中。AMO 用一条 RL 策略出 15 个下肢关节目标，SONIC 走整体跟踪，两者都以 500 Hz 给 G1 发关节命令。'), ('评测按三档随机化跑，成功是一条几何判据', 'DRManager 在每次 reset 时跑场景、目标与干扰物、空间、光照与材质随机化。成功由任务自带的 check_success 判定，都是几何阈值：抬离桌面 5 cm、放进容器、开门角度或推动距离过阈。每格十次记成 x/10，没有分阶段进度分，失衡如何计分论文没有给出。')],
 example='教学推演：回到上面的场景。第一步，这两条策略要在 SIMPLE 里比，先得进同一个环境：MuJoCo 按 500 Hz 算接触与平衡，Isaac Sim 按 50 Hz 渲出画面，观测与终止信号都从 Gymnasium 接口出来，谁也不能私下改物理。第二步，杯子、货架与房间都来自离线备好的资产——网格先做凸分解生成碰撞几何，落一遍确定稳定姿态，抓取位姿由 BoDex 提前合成并缓存，评测时不再现场算。第三步，演示怎么来决定了策略学到什么：自动通路由 CuRobo 出双臂轨迹、脚本管底盘，VR 通路让操作者只管上半身，平衡与迈步交给 AMO 或 SONIC，两条通路的腿都不是操作者一根根摆出来的。第四步，接口把控制权切开：策略交出的 36 维里，32 维是上半身目标，4 维是底盘速度与偏航，腿部关节自始至终在控制器手上，所以两条策略即使一强一弱，比的也只是这 36 维里的差别。第五步，评分是一条几何判据，抬离桌面 5 厘米、放进容器、开门角度过阈值，十次试验记成一个计数。这里要停一下：这套协议没有分阶段进度分，看不出失败发生在哪一步；摔倒与失去平衡怎么计，论文没有给出；§3.5 把三档随机化编号成 1 到 3，表 1 与附录却写成 0 到 2；仿真与真机相关的说法靠的是与已发表工作的排名对照，不是同一套任务同一套协议跑出来的。',
 code='env = gym.make("G1WholebodyXMovePickTeleop-v0", render_model="mujoco_isaacsim")\n# MuJoCo 500 Hz 物理 -> 同步状态 -> Isaac Sim 50 Hz 光追渲染\nobs, info = env.reset()      # DRManager 按档位跑场景/材质/光照/空间随机化\nwhile not (terminated or truncated):\n    a = agent.get_action(obs, info, instruction)   # 远程策略服务：公开检查点 + 作者微调\n    # a 共 36 维 = 上半身目标 32(双手14 + 双臂14 + 腰3 + 底盘高1) + 运动指令 4\n    upper, nav = a[:32], a[32:]                    # nav = vx, vy, vyaw, qyaw\n    wbc.apply_action(upper, nav)                   # AMO 或 SONIC：腿部关节由它出，500 Hz\n    obs, info, truncated, terminated = env.step(a)\nok = task.check_success()    # 几何阈值：抬离 5 cm / 放进容器 / 角度或距离过阈\n# 十次试验记成 x/10；没有分阶段进度分，失衡如何计分论文没有给出',
 setup='表 1 选六项代表任务、九条基线，每格按三档随机化报十次试验的成功计数；表 2 比三种采集通路的吞吐；表 3 与表 4 是 Ψ0 的数据配比与数据来源消融；表 5 是两项任务的零样本仿真到真机。',
 headers=['表 1 · 六项任务（三档随机化，各 10 次）', 'XMovePick', 'Handover', 'Mobile P&P'],
 rows=[['Ψ0', '10 / 10 / 6', '7 / 7 / 10', '7 / 5 / 6'], ['DreamZero', '10 / 10 / 10', '7 / 8 / 9', '5 / 3 / 3'], ['π₀.₅', '7 / 5 / 1', '5 / 4 / 5', '3 / 3 / 3'], ['GR00T N1.6', '10 / 10 / 7', '1 / 3 / 3', '0 / 0 / 0'], ['ACT', '10 / 10 / 5', '7 / 7 / 10', '5 / 5 / 5']],
 findings='同一条基线在不同任务上的落差远大于三档随机化带来的落差：GR00T N1.6 在 XMovePick 上是 10/10/7，在移动取放上六格全零。表 1 是六项任务的截面，不能压成一个人形策略成功率；附录表 7 里 OpenFaucet 只有 3–4/10，难度梯度是真的。',
 ablation='表 3 把 Ψ0 的训练数据换成 5 份 Level 0 加 5 份 Level 1，最难那档评测从 0.50 升到 0.80；把 XmoveBendPick 的遥操作演示从 10 条加到 100 条，三档到 1.00、0.90、0.90。表 4 里遥操作数据均分 7.56，运动规划 5.00。',
 boundary='模型来源要分三层看。仿真器、场景与物体资产、CuRobo、BoDex、CoACD 都是现成件；两条下肢控制器 AMO 与 SONIC 也是已发表的现成方法，作者只是把它们接进来。九条被评策略全部从公开检查点出发、由作者自己微调，而微调预算彼此并不相等：Ψ0 训 4 万步用 8 张 A100，GR00T N1.6 训 2 万步用 3 张，InternVLA-M1 冻住主干只训动作头，DP 与 ACT 直接从头训。所以表 1 的横向排名同时混进了策略本身与适配成本两件事。另有几处论文没有写清：§3.5 把三档随机化编号成 Level 1 至 3，表 1 与附录的 DRManager 却写成 Level 0 至 2；主文说从 GraspNet-1B 导入 53 个物体，附录写 75 个；失去平衡、摔倒或超时如何进入 check_success 没有规定；仿真与真机相关的说法靠的是与已发表工作的排名对照，不是同一套任务同一套协议。渲染只有约 4 帧每秒，物体一律按刚体建模。',
 transfer='对照 CaP-X：抽象层级与调试预算必须和成功率一起报，这里对应的是动作接口切在哪一维、以及每条基线各花了多少微调预算。',
 exercise='取任意两篇人形论文，把各自的动作维度、腿由谁产生、成功判据与试验次数填进同一张表再比。',
 answer='多数情况下会发现两张表比的不是同一件事。先把接口与预算对齐再谈方法收益，否则测到的是谁的控制器更强、谁的微调更充分。',
 sources=[('系统架构与评测协议：§3.1、§3.5', 'S3.SS1'), ('基线结果：表 1', 'S4.T1'), ('任务与成功判据：表 6', 'Sx1.T6'), ('全身控制器：附录 5.4', 'Sx1.SS4')])

add('openhlm', version='v1',
 takeaway='人形全身策略的瓶颈不在模型规模，在遥操作接口暴露了哪些自由度、便宜数据补得上什么。',
 result='长程取果任务进度 87.5%，Ψ0 48.8%、GR00T N1.6 57.5%；演示时长 1.14 小时对两条基线的 2.70 小时。',
 why='这篇把一条人形全身策略拆成可以逐个拨动的旋钮：遥操作接口、动作向量布局、主干从哪里初始化、便宜数据怎么混。对做编排的人，它给的是被调用那条策略的成本表——哪些能力靠现成预训练白拿，哪些必须自己采数据补。',
 scene='让人形去踩开垃圾桶踏板再把瓶子扔进去。按常见做法，上半身由逆运动学驱动，下半身交给一条只收底盘速度与根部高度指令的控制器；这套接口里没有一维能表达抬脚踩下踏板，于是这项任务不是做得差，而是从构造上就做不出来。',
 recall_module='改的是策略本身与它的数据来源，不是在策略外面加编排。动作向量从 21 维解耦指令换成 34 维全身关节目标，腿、腰与根部姿态第一次进入策略的输出；主干仍是现成的 π₀.₅，架构不动，只在投影层做权重手术。被换掉的另一半是数据：关节式全身遥操作取代了逆运动学加速度指令，定点遥操作与 HuMI 这两种便宜流混进同一个训练集，不改模型结构。SONIC 跟踪器、GMR 重定向与 PD 跟踪这一段全程没动，平衡仍由跟踪器负责。',
 recall_io='输入是头部鱼眼立体相机与两只腕部 RealSense 的图像、34 维本体状态（布局与动作向量一致）以及一句语言指令；四种遥操作接口共用同一套视觉与本体格式，区别只在动作那一侧。输出是一段 50 步的绝对关节目标动作块，34 维里前 32 维是双臂、双腿、腰部关节与根部 roll、pitch 和偏航角速度，后 2 维是两只平行夹爪；附录写的是按 30 Hz 生成、执行最多 25 步后重新推理，也就是每 5/6 秒一次。动作块交给 SONIC 全身跟踪器，再由 PD 控制器落到关节上，平衡与落脚由跟踪器自己处理。失败通路在这套系统里是隐式的：没有成功检测器，也没有恢复子目标，抓空之后策略只能在新观测上重新预测。论文给的证据是 π₀.₅ 初始化的策略会流畅地重抓，而 PaliGemma 初始化的很少从失败抓取里恢复——这条闭环纠错行为来自预训练数据，不是系统里某个模块。评分则完全在环外：人按细则给这次 rollout 打一个进度分。',
 steps=[('先定接口：四种遥操作各暴露多少自由度', '解耦控制 21 维：双臂 14、腰 3、根高 1、底盘速度 3，下肢归 RL 控制器；VR 三点 24 维，只给头与双腕位姿加导航；关节式全身 32 维，PICO 头显加身体追踪器采动作，经 GMR 重定向到 29 个关节；SMPL 式 81 维。四者共享同一套视觉与本体输入。'), ('对比结果决定了后面所有实验的数据来源', '表 1 里只有关节式把三项任务都做下来。解耦控制在可乐摆放上一趟走 42.3 步，是关节式 12.0 步的三倍，瓶子投放踩不了踏板，标为不可做；VR 三点则在罐前犹豫，时长涨到 67.8 秒、进度掉到 40%。另一面是货架取杯上解耦控制反而 93.3%，高于关节式的 80.0%。'), ('VLA 怎么改：接口细节不是瓶颈，初始化才是', '主干用 π₀.₅ 的公开检查点，内部架构不动。动作投影从 32 维扩到 34 维时做权重手术，保留前 32 维预训练权重；沿用预训练的双臂排布，腿、腰与根部姿态追加在末尾；预测绝对关节位置；本体状态作为输入。这四项各自翻转只掉几点，换成 PaliGemma 却掉到 60%。'), ('便宜数据分两种，补的东西不一样', '定点遥操作用同一台机器人、同一套关节式接口，只录站定不走的操作段，每项任务 40 条约 13 分钟；HuMI 是手持 UMI 夹爪加五个追踪器，机器人完全不在环里，40 条约 7 分钟，事后离线逆运动学抬成全身关节目标，动作还前移 0.2 秒对齐预览延迟。'), ('评分：每次 rollout 打一个进度分，不是二值成功', '每对策略与任务在真机上跑五次，五个初始摆放固定给所有策略共用。每项任务写好 3 到 5 分的评分细则，走位并返回起点算一分、抓对物体算一分、放到指定位置算一分，得分除以总分就是这一次的进度，论文报均值与标准误。摔倒或失去平衡如何计分，评分细则里没有写。')],
 example='教学推演：回到上面的场景。第一步，能不能踩踏板在接口那一步就定了：解耦控制给的是 21 维，腰与双臂加一个根高、一个底盘速度，腿上没有可写的量；关节式全身给的是 32 维，14 个手臂关节、12 个腿关节、3 个腰关节再加根部姿态，抬脚这件事才有地方表达。第二步，表 1 的证据不只是能不能做：解耦控制走同一段路花了 42.3 步，关节式只要 12.0 步；VR 三点在可乐罐前犹豫，时长涨到 67.8 秒。但另一面也要记住——货架取杯上解耦控制反而是 93.3%，高于关节式的 80.0%。第三步，模型这一侧的改动其实很小：π₀.₅ 的检查点照用，动作投影从 32 维扩到 34 维时保留前 32 维权重，腿腰与根部姿态追加在末尾，预测绝对关节位置。这四项各翻一项只掉几点，换成 PaliGemma 掉到 60%，随机初始化掉到 42%，所以真正吃紧的是机器人预训练本身。第四步，新物体和新指令不必再采全身遥操作：定点遥操作 40 条 13 分钟，HuMI 40 条 7 分钟，后者机器人完全不在环里，动作还要事后逆运动学抬成全身关节目标。两者都能补上语义，只有定点遥操作补得上倒水这种新动作。第五步，成绩按细则打进度分，走位并返回算一分、抓对算一分、放准算一分。这里要停一下：评分细则里没有摔倒这一条；长程对比里两条基线吃的是解耦控制采的演示，Ψ0 还被改接到 GR00T 的解耦栈上，那张表同时换了模型与接口两个变量。',
 code='policy = pi05_checkpoint()          # 现成：π0.5，预训练数据里没有任何人形数据\nresize_action_proj(policy, 32 -> 34, mode="weight_surgery")   # 保留前 32 维权重\ntrain(policy,\n      data = whole_body_teleop(8 个训练任务, 每项 40 条)        # 关节式 32 维 + 2 夹爪\n           + stationary_teleop(4 个留出任务) 或 HuMI(4 个留出任务))\n# AdamW, lr 1e-4, 30k 步, batch 128, 4 x A800 约 24 小时\n\nwhile 任务未完成:\n    o = (头部鱼眼立体 + 双腕 RealSense, 34 维本体状态, 语言指令)\n    chunk = policy(o)               # 50 步绝对关节目标，多步流匹配，不用单步\n    for a in chunk[:25]:            # 执行 25 步后重新推理，约每 5/6 秒一次\n        sonic_tracker(a)            # 现成跟踪器出关节目标，PD 跟踪，平衡归它\n# 没有成功检测器；抓空只能靠下一帧重新预测。评分在环外按细则打进度分',
 setup='HLM-12 是这篇自带的真机基准，12 项语言条件任务分四个能力族；路线图前段只用其中 4 项做消融，全套 VLA 定下来后扩到 8 项训练任务，共训阶段再加 4 项留出任务。表 2 换成另一项长程取果任务，20 对水果里均匀抽 10 对各跑一次。',
 headers=['表 2 · 长程取果任务', '进度', '演示时长'],
 rows=[['Ψ0', '48.8 ± 4.4%', '2.70 小时'], ['GR00T N1.6', '57.5 ± 4.6%', '2.70 小时'], ['OpenHLM（HuMI 共训）', '87.5 ± 3.7%', '1.14 小时'], ['OpenHLM（遥操作上限）', '97.5 ± 1.7%', '2.73 小时']],
 findings='87.5% 与上限 97.5% 之间还差 10 个点，而这 10 个点买的是 1.6 小时操作时间。更该记住的是两条基线的失败方式：它们走一条刻板的手臂轨迹，不跟语言指定的水果走；Ψ0 还在高架前停下，说明差距不只出在抓取。',
 ablation='图 5 的三组消融方向并不一致：接口四项各自翻转只掉几点，预训练这一项从 π₀.₅ 的 91% 掉到 PaliGemma 的 60%、随机初始化的 42%。单步生成的验证动作 MSE 反而更低，约 0.007 对 0.009，真机上却掉约 20 个点。',
 boundary='模型来源分得很清楚：π₀.₅ 的公开检查点、SONIC 全身运动跟踪器、GMR 重定向、PICO4U 与 HuMI 硬件全是现成件，而 π₀.₅ 的预训练数据里没有任何人形数据；作者训练的只有那一次微调，AdamW、3 万步、批 128、4 张 A800 约 24 小时。真正的前提成本是演示本身——每项任务 40 条关节式全身遥操作，熟练操作者约 1.5 小时。两处可比性要自己扣住。一是系统级对比里，GR00T N1.6 与 Ψ0 吃的是解耦控制采的 120 条演示，Ψ0 还被改接到 GR00T 的解耦栈上跑，作者说明这是为了避免再采一轮数据，于是那张表同时混进了模型与遥操作接口两个变量。二是 OpenHLM 那 1.14 小时里有 84 条来自 HuMI，与遥操作不是同类数据，时长不能当作同质预算直接比。另有没写清的地方：§3.1 说高层指令频率约 10 Hz，附录 C.4 却写成按 30 Hz 生成 50 步动作块、每 5/6 秒推理一次；评分细则里没有摔倒或失去平衡的条目；结论也承认只在 Unitree G1 上验证过。',
 transfer='对照 π₀.₅ 与 RT-2：同样是训练配方，这里多出来的变量是接口暴露多少自由度，以及便宜数据只补语义还是也补动作。',
 exercise='给自己的策略画一张表：动作向量每一维由谁产生，哪几维在评测里其实从未被真正用到。',
 answer='常见结果是有若干维一直是常数或被控制器覆盖。这类维度让动作空间看起来更大，却不改变可达行为，也让跨论文的维度对比失去意义。',
 sources=[('遥操作接口对比：§3.1、表 1', 'S3.T1'), ('VLA 设计与异构共训：§3.2、§3.3', 'S3.SS2'), ('长程系统级对比：§4、表 2', 'S4.T2'), ('HLM-12 任务与评分细则：附录 A.1', 'A1.SS1')])

add('omega0', version='v2',
 takeaway='未来预测不必长成一段视频：把它压成几个潜在嵌入，只当作动作学习的辅助监督。',
 result='11 项家务任务各 10 次试验，全视角变体成功率 81.8%、进度 90.3%，最强基线 DiT4DiT 为 43.6% 与 61.0%。',
 why='世界动作模型进入人形全身场景时有一个选择要做：未来到底要不要长成像素。这篇选了不长，用冻结编码器抽出的未来观测嵌入当回归目标，动作分支直接去噪出控制器吃得下的全身动作潜变量。对读编排的人，它给的是 WAM 后端在全身任务上的一种最小形态。',
 scene='让人形拖地。拖把要一直贴着地面，机器人同时得迈步、压低躯干、维持平衡。按先走后做的做法，策略会把这件事切成走到位再挥动手臂两段；一到需要边走边保持接触，两段之间的切换就变成停顿与来回修正，拖把也离地了。',
 recall_module='改的是动作生成这一层里未来信息的形态。它没有换控制器，SONIC 照旧接管平衡与落脚；也没有引入外部规划器、成功检测器或技能库。被换掉的是 WAM 常见的视频中枢：未来不再被生成成像素再反解成动作，而是由一组视频查询去回归冻结编码器抽出的未来观测嵌入，动作查询通过一次交叉注意力把这份预测吸进来，再由动作 DiT 直接去噪出控制器可执行的全身动作潜变量。另一处改动在数据侧：SONIC 仿真回放把人类动作换成机器人可执行的状态与潜变量对。',
 recall_io='输入是一句语言指令、一帧当前视觉观测与机器人本体状态。视觉那一路由冻结的 V-JEPA 抽特征，训练时可以是第一人称也可以是第三人称，靠一个可学习的视角 token 区分；本体状态是身体关节位置、灵巧手关节位置与躯干姿态，躯干姿态特意从四元数换成连续的 6D 旋转表示，以免双覆盖带来的跳变。输出有两路，但只有一路会落到机器人上：视频查询输出的未来视觉潜变量仅用于训练监督，需要看图时才另接一个解码器，推理与实时控制都不用它；动作 DiT 输出一块全身动作潜变量，交给 SONIC 换成全身控制命令。执行按滚动时域推进，上一块的末几帧被缓存成这一块去噪的干净前缀，训练时用随机 0 到 8 帧前缀先把这件事练过。失败没有专门通路：系统里没有成功检测器，也没有恢复动作，做砸了之后策略只能在新观测上重新预测下一块。评测侧的处理是把首次失败或不可逆偏离之后的部分不再计入进度，而子任务计分则允许早期出错后，仍为后面做对的子任务拿分。',
 steps=[('第一阶段：先让 VLM 学会说全身动作的话', '先在统一成 SMPL 的动作上训一个全身 FAST 分词器，把连续轨迹压成动作 token，重建误差走 L1。再用这些 token 当标签微调 Qwen3-VL-2B-Instruct，输入是一句指令、一张当前观测与一个视角 token。产出的隐藏表示后面当动作语义先验用。'), ('第二阶段：用仿真回放把人类数据换成机器人可执行的监督', '公共数据 ARCTIC、Xperience-10M 与 Motion-X 只有视频与人体动作，没有机器人状态与动作潜变量。作者把它们统一成 SMPL 后交给 SONIC 在仿真里批量回放，记录对应的全身动作潜变量与本体状态，跟不住的高动态轨迹丢掉，武术类动作大多在这一步被过滤。'), ('联合预测器把未来视觉塞进动作查询', '前缀由 VLM 特征、T5 文本、视角 token 与冻结 V-JEPA 的当前图像特征拼成。两组可学习查询各司其职：动作查询数等于动作块长度，视频查询对应未来视觉潜变量。两组先各自自注意力，再交叉注意前缀，最后动作查询注意视频查询。视频那一路的监督来自冻结 Wan 编码器。'), ('动作 DiT 去噪出 SONIC 吃得下的全身潜变量', '动作特征与文本特征、状态编码器给出的本体特征融合成条件，动作 DiT 以 x0 预测为目标去噪。第三阶段在真机数据上继续训，并加入训练时 RTC：随机取 0 到 8 帧前缀换成干净潜变量，损失只算非前缀那一段。V-JEPA、Wan 与第一阶段的 VLM 全程冻结。'), ('部署：滚动时域，前缀来自上一块', '每个决策步取头部相机一帧、机器人状态（身体关节、灵巧手关节与躯干姿态的 6D 旋转）与指令，用 DDIM 少步采样出一块动作潜变量，把上一次预测的末几帧当干净前缀以保持衔接，再交给 SONIC 换成全身控制命令。评测里没有独立的成功检测器，终止靠外部设定的时限。')],
 example='教学推演：回到上面的场景。第一步，拖地这件事要先变成模型说得出口的东西：作者把统一成 SMPL 的动作序列交给全身 FAST 分词器压成离散 token，再用它们微调 Qwen3-VL-2B-Instruct，让视觉语言主干学会把一句指令和一帧画面对上一串动作 token。第二步，真机演示不够，就借公共数据：ARCTIC、Xperience-10M 与 Motion-X 只有视频与人体动作，先统一成 SMPL，再让 SONIC 在仿真里逐条回放，跟得住的留下动作潜变量与本体状态，跟不住的丢掉，武术类动作大多死在这一步。第三步，联合预测器把未来接进来：前缀是 VLM 特征、T5 文本、视角 token 与冻结 V-JEPA 的当前图像，视频查询去回归冻结 Wan 编码器抽出的未来观测嵌入，动作查询再交叉注意视频查询，把这份预测吸进动作表示里。第四步，动作 DiT 以 x0 为目标去噪出全身动作潜变量，第三阶段加上训练时 RTC，随机 0 到 8 帧前缀换成干净潜变量、损失只算后半段。第五步，部署时每步取一帧头部画面与一份本体状态，DDIM 少步采样出一块潜变量，上一块的末几帧当干净前缀保持衔接，SONIC 把潜变量换成全身控制命令，拖把因此能在迈步过程中一直贴着地。这里要停一下：没做到的是判定这一层。系统里没有成功检测器，做砸了只能靠下一帧重新预测；子任务如何划分论文说在附录，而这一版没有附录；评测时限与摔倒计分同样没有写。',
 code='# Stage 1: FAST 分词器(自训) + Qwen3-VL-2B 微调 -> 全身动作 token\n# Stage 2: 公共人类数据 -> 统一 SMPL -> SONIC 仿真回放 -> (状态, 动作潜变量)，跟不住的丢弃\np = concat(f_vlm, f_T5(l), view_token, f_VJEPA(o_t))    # V-JEPA / Wan / VLM 全程冻结\nq_m, q_v = motion_queries, video_queries                # 数目 = 动作块长 / 未来潜变量\nq_m, q_v = cross(q_m, p), cross(q_v, p)\nh_m = cross(q_m, q_v)                                   # 未来视觉注进动作表示\nL = ||DiT(z_t, t | fuse(h_m, f_T5, f_state)) - z0||^2 + lam * ||q_v - Wan(o_future)||^2\n\nwhile 任务未完成:                                        # 部署\n    z = ddim_denoise(prefix = cache[-M:])               # RTC：上一块末几帧当干净前缀\n    sonic(z)                                            # 动作潜变量 -> 全身控制命令\n    cache = z\n# 没有成功检测器；进度按首次失败或不可逆偏离之前的阶段数在环外统计',
 setup='ω-HOME 是这篇自采的 40.3 小时、24 项任务数据集，下游 11 项家务全身评测任务从中选出并排除在第二阶段预训练池外。每项跑 10 次，所有方法都在同一批真机数据上训成多任务模型、按同一协议评；指标为整任务成功率、子任务计分与首次失败前的归一化进度。',
 headers=['表 2 · 11 项家务任务', '成功率', '计分（上限 41）', '进度'],
 rows=[['π-0.5', '27.3%', '20.9', '52.8%'], ['ψ-0', '44.5%', '23.6', '59.6%'], ['DiT4DiT', '43.6%', '23.1', '61.0%'], ['ω-0 Ego', '79.1%', '35.8', '88.7%'], ['ω-0 Omni', '81.8%', '36.7', '90.3%']],
 findings='成功率与进度这两列拉开的幅度不一样：DiT4DiT 进度已有 61.0%，成功率只有 43.6%，说明失败集中在靠后的阶段。ω-0 把两列一起抬到 81.8% 与 90.3%，但这 11 项任务是作者自建的，不是公开基准；ψ-0 用的下层控制器也是 AMO 而不是 SONIC。',
 ablation='表 4 里掉得最多的是去掉本体状态，成功率降到 60.9%；其次是把当前图像换成 Wan 编码器的 63.6% 与去掉视频查询的 64.5%，去掉 RTC 掉到 71.8%。表 5 更能说明视频查询的作用：跨场景那一栏没有它时只剩 15.0%。',
 boundary='模型来源几乎全是现成件：Qwen3-VL-2B-Instruct、V-JEPA 2.1 图像编码器、Wan 视频编码器、T5 文本编码器、SONIC 全身控制器与 FAST 动作分词方案都取自已有工作，其中前几项在第二、三阶段全部冻结。作者训练的是全身 FAST 分词器、第一阶段的 VLM 微调，以及联合视频—动作潜变量预测器、状态编码器、条件融合模块与动作 DiT，三阶段都在 8 张 H100 上跑；真正的前提成本是 40.3 小时、24 项任务的 ω-HOME 数据，加上下游 11 项任务每项约 200 条演示。有几处论文没有给出：动作潜变量的维度与它同 SONIC 参考动作的对应关系没有写；每项任务的子任务划分说在附录里，而这一版没有附录；评测时限与摔倒如何计分同样没有规定。可比性上还有两处要扣住：全视角变体在五项移动量大的任务上吃的是房间里那台外置相机的画面，论文只把外视角描述成训练期的补充监督，没有说明部署时这台相机是否必需；GR00T-N1.7 因为公开推理管线没有 RTC 而按普通顺序分块评测，而表 4 显示 RTC 约值 7 个点。',
 transfer='对照 EMERGE-Policy 里用的 WAM 后端：那边把想象与评估拆成两个技能，这边把未来压进潜变量，推理时不生成视频。',
 exercise='在自己的策略上加一个未来嵌入回归头，比较只看当前观测与加上未来监督两种版本的跨场景表现。',
 answer='如果收益只出现在同分布任务上，那更像是多了一个正则项；这篇的证据恰好在跨场景与人类数据迁移那两栏最明显。',
 sources=[('三阶段方法：§3', 'S3'), ('ω-HOME 数据集：§4', 'S4'), ('主结果：表 2', 'S6.T2'), ('消融与泛化：表 4、表 5', 'S6.T4')])

add('wholebodywam', version='v1',
 takeaway='不同全身控制器的指令语义不一样；先给它们一张共用的槽位表，预训练的操作先验才搬得过去。',
 result='SIMPLE 六项仿真任务整体成功率 91.9%，Cosmos-3 86.4%、Ψ0 82.2%；真机八项分布外 68.8% 对 40.0%。',
 why='这一篇的切口不在模型更大，而在接口：三条全身控制器对同一个动作的指令写法不同，策略学到的语义就没法复用。它把这些指令排进一张有固定物理含义的槽位表，再让操作意图在手臂能力不够时去牵引全身指令。',
 scene='同一条把托盘端起来的动作，换一台全身控制器就要重写：一台收下肢关节参考，一台收底盘速度加根部高度，一台还要一个转向标志位。把这些量直接拼成一个平铺的动作向量喂给策略，换控制器就得重学一遍指令约定，预训练学到的操作先验一点也搬不过去。',
 recall_module='改的是动作输出的组织方式，不是世界模型本身，也不是任何一条全身控制器。预训练 WAM 的视觉到操作通路原样保留，另开一条面向控制器的 UWBC 流，两者与视觉流在同一个扩散 Transformer 里联合生成。被换掉的是把异构控制器指令平铺成一个动作向量的做法：改成一张 46 维共享槽加 10 维残余槽的语义表，不支持的字段掩掉。再加一个按方向可操作度开合的注意力偏置，只在手臂于任务方向上能力不足时才加强操作到 UWBC 的信息流。控制器仍然自己负责平衡与落脚。',
 recall_io='输入是视觉历史、当前机器人状态与一句语言指令：视觉经预训练的 Wan 视频 VAE 压成潜在视频 token，文本走预训练 T5 编码器再以交叉注意力进入，机器人状态由一个轻量状态编码器变成本体条件 token。输出是三条并行的流，每个未来步共享一个预测块：未来视觉动态、操作动作（手臂关节目标与手指关节角）、以及 56 维 UWBC 指令。落到硬件上的只有后两条——UWBC 按目标控制器的能力档案激活对应字段，其余掩掉，再交给 SONIC、AMO 或 GEAR WBC 换成实际关节命令，平衡与落脚仍归控制器。失败在系统里没有显式通路：没有成功检测器，没有重试逻辑，也没有恢复子目标。论文报的唯一一次恢复是分布外的观察——托盘支架被挪远 10 厘米后一次伸手没够着，模型接着向前迈一小步、调整躯干再抓一次；作者说明这个序列不在演示里，把它归为预训练操作先验与全身协调耦合后的涌现行为，而不是某个模块的设计结果。评测侧的失败处理是外部的：进度按首次失败或不可逆偏离之前的阶段数来算。',
 steps=[('三条类型化流共用一个扩散 Transformer', '视觉 token、操作 token 与 UWBC token 在每个未来步共享一个预测块，由同一个扩散 Transformer 联合生成。操作流保留预训练 WAM 原有的视觉到操作通路，出手臂关节目标与手指关节角；UWBC 流是新加的，让面向控制器的全身行为单独可寻址。'), ('UWBC：46 维共享槽加 10 维残余槽', '共享槽的物理含义写死：底盘线速度与角速度各 3 维、朝向正余弦 2 维、骨盆平面位置 2 维、骨盆高度 1 维、骨盆倾角 2 维、躯干相对骨盆姿态 3 维，其后是 15 维下肢与腰部关节位置、15 维关节速度。索引 46 到 56 留给各控制器自己的量，不支持的字段一律掩掉。'), ('三条控制器各自落在哪些槽位上', 'SONIC 用关节空间的 16 到 46 槽；AMO 把平面速度映到 0 到 2、朝向映到 6 到 8、绝对高度映到第 10 槽、躯干姿态映到 13 到 16，转向标志进残余槽；GEAR WBC 的偏航角速度放在第 5 槽。每换一个控制器重做一次任务级微调，只激活演示里有的字段。'), ('CASA：按方向可操作度决定要不要动全身', '先从相邻两帧末端位置估出任务方向，再用带阻尼的可操作度矩阵算出该方向上的能力，与离线体素化可操作度图的参考值比，得到 0 到 1 的下降比例，左右手取较大者作门控。门控乘一个固定强度加成注意力 logit 偏置，只作用在同一块内操作键到 UWBC 查询这一个方向上。'), ('后训练与部署预算', '从 140 亿参数的预训练 WAM 出发，在 SIMPLE 里用 PICO 4 Ultra 与腕部追踪器采一万五千条全身演示，每条经 GMR 重定向到 G1，再按各槽位预定义的语义算出监督值。只训一层秩 4 的 LoRA，5 万步；下游每项任务再用 100 条演示微调。')],
 example='教学推演：回到上面的场景。第一步，端托盘这件事被拆成三条流：视觉流预测未来画面，操作流沿用预训练 WAM 原有的通路出手臂关节目标与手指角，UWBC 流单独负责面向控制器的全身指令，三者在同一个扩散 Transformer 的同一个预测块里联合生成。第二步，UWBC 把各家控制器的方言收进一张表：前 46 维物理含义写死，底盘线速度与角速度、朝向正余弦、骨盆平面位置与高度、骨盆倾角、躯干相对骨盆姿态，再接 15 维下肢腰部关节位置与 15 维速度；剩下 10 维残余槽留给各家自己的量，包括离散控制模式。第三步，换控制器就是换激活哪些槽：SONIC 吃 16 到 46 的关节空间，AMO 只吃平面速度、朝向、绝对高度与躯干姿态，转向标志进残余槽，GEAR WBC 把推导出的偏航角速度放在第 5 槽，没被支持的字段一律掩掉。第四步，托盘偏远、手臂在任务方向上够不动的时候，CASA 才起作用：门控按可操作度相对参考值的下降比例开合，乘一个固定强度加到注意力 logit 上，只加强同一块内操作到 UWBC 这一个方向，于是全身跟着操作意图动起来。第五步，这一切都建在现成件上，训练只有一层秩 4 的 LoRA 跑 5 万步，加上一万五千条在 SIMPLE 里采的演示。这里要停一下：偏置强度、门控常数、模态损失权重与体素图分辨率论文都没有给出，真机八项任务的成功判据与阶段划分同样没有给出，真机对照也只有 DreamZero 一条。',
 code='backbone = pretrained_WAM_14B()      # 现成：世界-动作先验，只加一层 LoRA(r=4) 适配\nu = zeros(56)                        # UWBC：46 维共享槽 + 10 维残余槽\nu[0:3], u[3:6] = base_lin_vel, base_ang_vel\nu[6:8], u[8:10], u[10] = heading_sin_cos, pelvis_xy, pelvis_height\nu[11:13], u[13:16] = pelvis_tilt, torso_to_pelvis\nu[16:31], u[31:46] = lower_q, lower_qdot     # 下肢与腰部关节位置 / 速度\nu[46:56] = controller_specific               # 含离散控制模式\n\ng = max_over_arms(1 - clip(c_dir / c_ref_voxel, 0, 1))      # 方向可操作度下降比例\nlogits = QK^T/sqrt(d) + M_nat + beta * g * S_manip_to_uwbc  # 只加强这一个方向\nv, m, u = DiT_joint(视觉历史, 机器人状态, 语言指令)            # 三流同块联合流匹配\nwbc.send(mask_by_capability(u, target_wbc), m)              # SONIC / AMO / GEAR\n# 没有成功检测器与重试；分布外那次踏步重抓，论文称为涌现而非模块设计',
 setup='仿真在 SIMPLE 上跑六项任务、三档扰动，表 2 报的是配 SONIC 时的整体成功率；真机是 Unitree G1 配两只 BrainCo Revo 2 灵巧手、头部与双腕 RealSense，八项任务分同分布与分布外两档。每个方法与任务的组合跑 20 次。',
 headers=['表 2 · SIMPLE 六项任务（配 SONIC）', 'MovePick', 'PickBetweenTables', '整体'],
 rows=[['DreamZero', '70/65/55', '50/45/35', '65.0%'], ['Cosmos-3', '90/85/80', '85/80/70', '86.4%'], ['Ψ0', '90/85/75', '75/70/60', '82.2%'], ['WholeBodyWAM', '95/90/85', '95/90/80', '91.9%'], ['去掉结构化动作分解', '85/80/70', '80/75/65', '80.8%']],
 findings='同分布下操作主导的任务差距很小，桌面收拾是 90% 对 85%；需要大幅移动躯干的任务差距才拉开，箱体转运差 40 个点，篮筐搬运与门口通过各差 35 个点。分布外的掉幅也不同，12.5 个点对 17.5 个点。',
 ablation='六项仿真任务上，去掉 CASA 从 91.9% 掉到 86.9%，把 UWBC 换回 SONIC 原生指令掉到 84.7%，把操作流与 UWBC 流合成一条容量相当的单一流掉到 80.8%。掉得最多的是最后这项，说明分开寻址比接口语义本身更要紧。',
 boundary='模型来源几乎全部现成：主干是那条 140 亿参数的预训练 WAM，文本编码器 T5、视频 VAE Wan、重定向工具 GMR、仿真环境 SIMPLE，以及被对齐的三条全身控制器 SONIC、AMO 与 GEAR WBC 都不是这篇训的。作者训练的只有 LoRA 那一层适配与一个轻量状态编码器，加上一万五千条在 SIMPLE 里采的全身演示与每项任务 100 条下游演示，这两笔数据才是真正的前提成本。把成绩读成方法收益之前有几处要扣住：CASA 的偏置强度、门控里的小常数、各模态损失权重与体素图分辨率论文都没有给出；真机那八项任务的成功判据与阶段划分也没有给出，而进度这一指标是直接沿用 ω-0 的定义；真机对照只有 DreamZero 一条；跨控制器那组是每条控制器各自单独微调后再比的。表 2 的格子来自 SIMPLE 的三档扰动，而 SIMPLE 原文里同名任务报的是十次试验的计数，这里换成了百分比与每组 20 次，两张表不能直接并排。分布外那次踏步重抓，论文明说演示里没有、称之为涌现行为，属于单例观察。',
 transfer='对照 RoboHarness 的交接状态：那边要把上一条策略留下的构型推进下一条的支撑区，这边要把同一份意图翻译成不同控制器认得的槽位。',
 exercise='给手上两套控制器各写一张槽位表，标出哪些字段真的共享语义、哪些只能放进残余槽。',
 answer='多数情况下共享的字段比预想的少，差异集中在朝向与高度的参考系上。先把参考系对齐，再谈跨控制器复用。',
 sources=[('UWBC 槽位表：§III-C、表 1', 'S3.T1'), ('CASA 与门控：§III-D', 'S3.SS4'), ('仿真结果与消融：表 2', 'S4.T2'), ('真机同分布与分布外：表 3', 'S4.T3')])
